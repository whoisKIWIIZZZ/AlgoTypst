#import "../lib.typ": *
#let problem = remark
= chapter9: 分支限界法
<chapter9-分支限界法>

== 引言
<引言-分支限界>

=== 分支限界与回溯的异同
<分支限界与回溯的异同>

#definition[分支限界法][
  分支限界法（branch and bound）和回溯法一样，都是通过系统地搜索解空间树来求解的算法。
  区别在于搜索方式：

  - *回溯法*：深度优先搜索（DFS）遍历状态空间树，用约束函数和限界函数剪枝
  - *分支限界法*：广度优先或最小成本优先搜索，每访问一个 E-结点，生成它的所有儿子结点，再利用限界函数剪枝

  回溯法用栈（递归栈或显式栈）管理活结点，分支限界法用队列（FIFO）或优先队列（LC）管理活结点。
]

=== 三种检索策略
<三种检索策略>

分支限界法根据活结点表的数据结构分为三种检索策略：

+ *FIFO 检索*：活结点表采用普通队列（first in first out）。每次取出队首结点作为 E-结点，生成的所有儿子入队尾。等价于 BFS 的层序遍历，选择方式死板、盲目。
+ *LIFO 检索*：活结点表采用栈（last in first out）。每次取出栈顶结点作为 E-结点，新儿子压栈。类似于 D-检索。
+ *LC-检索（最小成本检索）*：对每个活结点计算成本估计值 $hat(c)(dot.op)$，从优先队列中选取 $hat(c)$ 最小的活结点作为下一个 E-结点。使最有可能通向答案的结点优先被扩展。

=== 成本估计函数
<成本估计函数>

#definition[结点成本函数 $c(dot.op)$][
  对于状态空间树上的结点 $X$：
  - 若 $X$ 本身是答案结点，则 $c(X)$ 是从根到 $X$ 的路径成本
  - 若 $X$ 不是答案结点且子树 $X$ 不含任何答案结点，则 $c(X) = infinity$
  - 否则 $c(X)$ 等于子树 $X$ 中具有最小成本的答案结点的成本
]

然而，精确计算 $c(X)$ 需要搜索子树 $X$——和解原问题一样困难。因此引入成本估计函数 $hat(c)(X)$：

$ hat(c)(X) = f(h(X) + hat(g)(X)) $

其中：
- $h(X)$ 是从根结点到结点 $X$ 的已发生成本（已知）
- $hat(g)(X)$ 是从 $X$ 到最近答案结点所需附加成本的估计（启发式）
- $f$ 是一个非降函数，用于平衡纵深搜索与宽度搜索

若 $X$ 是答案结点或叶子结点，则 $c(X) = hat(c)(X)$。

=== 15-谜问题
<15-谜问题>

#problem[15-谜问题][
  在一个 $4 times 4$ 的棋盘上放有 15 块编号为 1 至 15 的牌和一块空格，每次只允许将空格相邻的牌移到空位上。给定一个初始排列，判断能否通过合法移动到达目标排列（按序排列，空格在右下角）。
]
#image("../assets/9-image-4.png")
==== 可达性判定：奇偶不变量

并非所有排列都能通过合法移动还原。可达性由以下定理判定：

#definition[可达性判定][
  给 $4 times 4$ 棋盘方格编号 $1 tilde.op 16$，位置 $i$ 是目标排列中放 $i$ 号牌的方格位置，位置 16 是空格位置。设：

  - $P O S I T I O N (i)$：编号 $i$ 的牌在初始状态下的位置号（$1 <= i < 16$），$P O S I T I O N (16)$ 为空格的位置号
  - $L E S S (i)$：满足 $j < i$ 且 $P O S I T I O N (j) > P O S I T I O N (i)$ 的 $j$ 的数目（实质是逆序对计数）
  - $X$：若初始空格在棋盘阴影区域中（通常为 $("行" + "列")$ 和为奇数的方格），则 $X=1$；否则 $X=0$

  *定理*：当且仅当 $sum_(i=1)^16 L E S S (i) + X$ 为偶数时，目标状态可由初始状态到达。
]

#remark[
  这个判定的数学基础是：$sum L E S S (i) + X$ 的奇偶性是合法移动下的*不变量*。
  横向移动交换空格与相邻非空白牌，同时改变 $L E S S$ 的奇偶性；纵向移动跨行排列时 $L E S S$ 和 $X$ 同时翻转，二者抵消。
  因此一旦初始状态算出的总和为奇数，可立即判定无解，无需搜索。
]

==== 三种检索策略对比

PPT 分别演示了 FIFO、DFS、LC 三种策略处理 15-谜问题：

- *FIFO 检索*：一层层展开所有可能移动，结点膨胀极快，无方向性
- *DFS 检索*：沿某一分支深入，可能长时间找不到解
- *LC-检索（最优）*：定义 $hat(c)(X) = h(X) + hat(g)(X)$，其中：
  - $h(X)$ = 根到结点 $X$ 的路径长度（实际已走步数）
  - $hat(g)(X)$ = *不在其目标位置的非空白牌数目*（易于计算的启发式）

  可以证明 $hat(c)(X) <= c(X)$，即 $hat(c)$ 是 $c$ 的*下界*。LC-检索据此优先扩展最可能接近目标的结点。

== LC-分支限界策略
<LC-分支限界策略>

=== 基本思想
<LC-分支限界基本思想>

LC-分支限界的核心策略：

+ 使用满足 $hat(c)(X) <= c(X)$ 的成本估计函数 $hat(c)$，给出从任一结点 $X$ 出发的解的下界
+ 维护当前最优解的成本*上界* $U$（初始可置 $infinity$ 或用启发式估计）
+ 剪枝条件：若 $hat(c)(X) > U$，则结点 $X$ 可以被杀死——因为 $c(X) >= hat(c)(X) > U$，其子树不可能包含优于当前解的解

#remark[
  LC-检索与 FIFO/LIFO 的本质区别在于取下一个 E-结点时不是机械地取头/取顶，而是按 $hat(c)$ 值排序，让最有希望的结点领先扩展。这就是"有智力的"搜索。
]

=== LC-SEARCH 算法框架
<LC-SEARCH算法框架>

LC-检索的通用过程如下：从根结点 $T$ 开始。若 $T$ 本身就是答案结点则直接输出并结束。否则将 $T$ 设为初始 E-结点，初始化活结点表为空。然后进入循环：对当前 E-结点 $E$ 的每个儿子 $X$，若 $X$ 是答案结点则输出从 $X$ 到 $T$ 的路径并返回，否则将 $X$ 加入活结点表并记录其父结点。若所有儿子处理完毕后活结点表为空，则输出"无解"并返回。否则从活结点表中选取 $hat(c)$ 值最小的活结点作为新的 E-结点，重复上述过程，直到找到答案结点或活结点耗尽为止。

== 经典应用
<经典应用-分支限界>

=== 货郎担问题（TSP）
<货郎担问题>

#problem[货郎担问题（旅行商问题 TSP）][
  给出 $n$ 个城市的集合和一个定义在每对城市间的耗费函数（可以是距离、时间、票价等非负值），
  找出一个闭合回路，使每个城市恰好被访问一次且总耗费最小。
  解空间为排列树，包含 $(n-1)!$ 条可能的回路。
]

==== 归约矩阵

#definition[归约矩阵][
  对耗费矩阵 $A$，每一行减去该行的最小值、每一列减去该列的最小值，使每行每列至少有一个 0 元素。
  因为每条周游路线在每行每列恰好选一条边，行/列减去常数 $r$ 会使所有周游路线的总耗费减少 $r$。

  总归约代价 = 所有行归约代价之和 + 所有列归约代价之和，且为最优解的*下界*。
]
#figure(
  grid(
    columns: (1fr, 1fr),
    image("../assets/9-image-5.png"),
    image("../assets/9-image-6.png",width:80%)
  ),
)

PPT 例子（5 城市）总归约代价为 63，即根结点 $hat(c)("根") = 63$。

==== 边的选择与惩罚值 $r_R$

对归约矩阵中每个值为 0 的边 $(i,j)$，计算*强制不选该边的惩罚*：

$ r_R = min_(k != j) {A(i,k)} + min_(k != i) {A(k,j)} $

含义：不选 $(i,j)$ 时，城市 $i$ 必须选择该行其他出边（最小值为第一项），
城市 $j$ 必须从其他城市接收入边（最小值为第二项）。

#table(
  columns: (1fr, 1fr),
  align: (center + horizon, left + horizon),
  [Cost(i,j)=0], 
  [不含 (i,j) 时，代价增量],
  
  [(1,3)], [Cost(1,2) + Cost(2,3) = 10 + 0 = 10],
  [(2,3)], [Cost(2,1) + Cost(1,3) = 4 + 0 = 4],
  [(3,5)], [Cost(3,2) + Cost(1,5) = 12 + 11 = 23],
  [(4,2)], [Cost(4,3) + Cost(5,2) = 4 + 1 = 5],
  [(5,1)], [Cost(5,4) + Cost(2,1) = 0 + 4 = 4],
  [(5,4)], [Cost(5,1) + Cost(2,4) = 0 + 6 = 6],
)

#remark[
  $r_R$ 是算法中的*关键复用量*：选边时用它判断哪条 0 边最适合做分裂点（$r_R$ 越大 → 不选它的惩罚越高 → 这条边越可能是最优解中的必经之路）；右儿子的成本公式中也用 $r_R$ 作为增量来抬高右子树的 $hat(c)$，迫使搜索优先走左子树（包含该边的方向）。

  PPT 中图例对根结点选 (3,5) 边时 $r_R=23$，但右子结点的矩阵漏掉了列归约（第 5 列未归约），导致图上 bound 标注为 75，实际应为 $63+23=86$。考试时需注意：$r_R$ 包含*行和列两项*，缺一不可。
]

==== 左右儿子的估计成本

以边 $(i,j)$ 分裂后：

*左儿子 $S_L$（包含 $(i,j)$）*：
- 删第 $i$ 行（城市 $i$ 已出发）和第 $j$ 列（已到达城市 $j$）
- 禁止回头：$A(j,i) arrow.l infinity$
- 对不全为 $infinity$ 的行列重新归约，得归约代价 $r_L$

$ hat(c)(S_L) = hat(c)(R) + A(i,j) + r_L $

*右儿子 $S_R$（不含 $(i,j)$）*：
- $A(i,j) arrow.l infinity$（禁止选该边）
- 重新归约第 $i$ 行和第 $j$ 列，得归约代价 $r_R$

$ hat(c)(S_R) = hat(c)(R) + r_R $

==== 搜索流程

#algorithm[TSP 分支限界搜索][
  *输入*：$n times n$ 耗费矩阵
  *输出*：最小成本周游路线

  ```text
  1.  归约矩阵，得总归约代价作为根 hat(c)
  2.  根入优先队列（按 hat(c) 排序）
  3.  repeat
  4.      取出 hat(c) 最小的 E-结点
  5.      在 E 的归约矩阵中找出所有 0 边，计算每条边的 r_R
  6.      选取 r_R 最大的 0 边 (i,j) 作为分裂边
  7.      生成左儿子（包含 (i,j)）：删行删列 → 归约 → 算 hat(c) → 入队
  8.      生成右儿子（不含 (i,j)）：置 infinity → 归约 → 算 hat(c) → 入队
  9.  until 某叶结点的 hat(c) ≤ 所有活结点的 hat(c)
  ```

  每次分裂锁定一条"必须走"或"必须不走"的边，深度最多 $n$ 层可得完整解。
  由于 LC-检索始终扩展 $hat(c)$ 最小的结点，且 $hat(c)$ 是下界，故首次到达的叶结点即为最优解。
]


=== 0/1背包问题
<01背包问题>

#problem[0/1背包问题][
  有 $n$ 个物品，第 $i$ 个物品重量 $w_i$、价值 $v_i$。背包容量为 $W$。
  每个物品要么选中（1）要么不选（0），求总重量不超过 $W$ 时总价值最大的方案。
  解空间为子集树，共 $2^n$ 个叶子。
]

先按 $v_i/w_i$ 非增序排列物品，排序耗时 $O(n log n)$

==== 上界函数 bound()

0/1 背包是最大化问题，需用*上界*限界。思路是：假设剩余物品可以按任意比例装入——即分数背包松弛，其最优解 ≥ 0/1 背包的最优解，故是合法上界。

#algorithm[bound() 上界函数][
  *输入*：当前结点 $e$（已装重量 $e.w$，已装价值 $e.v$，层次 $e.i$）
  *输出*：上界值 `e.ub`

  ```python
  def bound(e):
      i = e.i + 1
      sumw = e.w
      sumv = e.v
      while i <= n and sumw + w[i] <= W:
          sumw += w[i]
          sumv += v[i]
          i += 1
      if i <= n:                  # 剩余物品只能部分装入
          e.ub = sumv + (W - sumw) * v[i] / w[i]
      else:                       # 剩余物品全部能装入
          e.ub = sumv
  ```
]

==== 剪枝条件

+ *左剪枝（约束函数）*，选当前物品：$e.w + w[e.i+1] <= W$，否则超重剪枝
+ *右剪枝（限界函数）*，不选当前物品：$e_2 dot "ub" > "maxv"$ 才保留，否则不可能超过当前最优解

==== 优先队列式分支限界法

优先队列式使用大根堆（按 $"ub"$ 排序），$"ub"$ 越大越先出队。

#algorithm[0/1背包优先队列式][
  *输入*：$n$, $W$, $w[1..n]$, $v[1..n]$
  *输出*：最大价值 `maxv` 及最优解向量 `bestx[1..n]`

  ```python
  def bfs():
      e = Node(i=0, w=0, v=0, x=[0]*(n+1))
      bound(e)
      heap = [(-e.ub, e)]          # 大根堆（存负值实现）

      while heap:
          _, e = heappop(heap)     # 取 ub 最大的结点

          if e.w + w[e.i+1] <= W:  # 左孩子
              e1 = Node(i=e.i+1, w=e.w+w[e.i+1], v=e.v+v[e.i+1],x=e.x.copy())
              e1.x[e1.i] = 1
              bound(e1)
              if e1.i == n:        # 叶子
                  if e1.v > maxv:
                      maxv = e1.v; bestx = e1.x
              else:
                  heappush(heap, (-e1.ub, e1))

          # 右孩子
          e2 = Node(i=e.i+1, w=e.w, v=e.v, x=e.x.copy())
          e2.x[e2.i] = 0
          bound(e2)
          if e2.ub > maxv:         # 右剪枝
              if e2.i == n:
                  if e2.v > maxv:
                      maxv = e2.v; bestx = e2.x
              else:
                  heappush(heap, (-e2.ub, e2))
  ```
]



==== 复杂度

无论队列式还是优先队列式，最坏情况均需搜索整个解空间树，时间和空间复杂度均为 $O(2^n)$。


=== 最大团集问题
<最大团集问题>

#problem[最大团集问题][
  给定无向图 $G=(V,E)$。
  - *团*：顶点子集 $U subset.eq V$，其中任意两顶点间都有边
  - *极大团*：不被更大的完全子图包含的团
  - *最大团*：顶点数最多的团

  解空间为子集树，共 $2^n$ 个叶子（每个顶点选或不选）。
]

==== 上界函数

$ "upperSize" = "cliqueSize" + n - "level" + 1 $

其中 $"cliqueSize"$ 为当前团已包含的顶点数，$"level"$ 为当前层数（从 1 开始），$n - "level" + 1$ 为剩余未决定的顶点数。这是一个乐观估计——假设剩余顶点全部可以加入团中。

==== 优先队列式分支限界法

#algorithm[最大团优先队列式][
  *输入*：邻接矩阵 $"a"[1..n][1..n]$（$a[i][j]=1$ 表示有边）
  *输出*：最大团大小 `bestn` 及顶点选择向量 `bestx[1..n]`

  ```python
  def bound(cliqueSize, level):
      return cliqueSize + n - level + 1

  def bfs():
      heap = [(0, 0, 0, [0]*(n+1))]   # (-upperSize, level, cliqueSize, x)
      bestn = 0

      while heap:
          _, level, cliqueSize, x = heappop(heap)

          if level > n:               # 到达叶子
              if cliqueSize > bestn:
                  bestn = cliqueSize
                  bestx = x.copy()
              continue

          # 左儿子：选顶点 level
          ok = True
          for j in range(1, level):
              if x[j] == 1 and a[level][j] == 0:
                  ok = False          # 与已选顶点无边相连
                  break
          if ok:
              x1 = x.copy()
              x1[level] = 1
              ub = bound(cliqueSize + 1, level + 1)
              heappush(heap, (-ub, level+1, cliqueSize+1, x1))

          # 右儿子：不选顶点 level
          ub = bound(cliqueSize, level + 1)
          if ub > bestn:               # 右剪枝
              x2 = x.copy()
              x2[level] = 0
              heappush(heap, (-ub, level+1, cliqueSize, x2))
  ```
]

上界函数 $"upperSize" = "cliqueSize" + n - "level" + 1$ 剪枝能力较弱，但计算简单。


=== 圆排列问题
<圆排列问题>

#problem[圆排列问题][
  给定 $n$ 个圆的半径 $r[1..n]$（可能相等），将它们排成一行，使每个圆均与底边相切。求一种排列，使所有圆在水平方向上的总宽度最小。
#figure(
  grid(
    columns: (1fr, 1fr),
    image("../assets/ch9-circle-arrangement-1.png"),
    image("../assets/ch9-circle-arrangement-2.png",width: 50%),
  ),
)
]

==== 圆心横坐标计算

两个相切圆 $i$ 和 $j$ 的圆心水平距离由勾股定理得出：$2 sqrt(r_i r_j)$。第 $i$ 个圆的圆心坐标取它与所有已排圆相切所得的最大值。

==== 总宽度计算

#algorithm[总宽度计算][
  ```python
  def compute():
      low = 0
      high = 0
      for i in range(1, n+1):
          if x[i] - r[i] < low:
              low = x[i] - r[i]
          if x[i] + r[i] > high:
              high = x[i] + r[i]
      if high - low < min:
          min = high - low        # 更新最优解
  ```
]

==== 三种剪枝策略

+ *镜像对称剪枝*：排列 $(1,2,dots,n)$ 与 $(n,dots,2,1)$ 互为镜像，总宽度相同。仅需计算 $n!/2$ 种排列，即固定第一个圆，在 $2$ 到 $n-1$ 之间排列。

+ *相同半径剪枝*：若有 $k$ 个圆半径相同，互换位置不改变总宽度，只计算一个。交换前先判断半径是否相等。

+ *下界约束加强*：对已排 $m$ 个圆的状态：

  $ "min_len"(m) + 2 sqrt(r_m r_(m+1)) + "len_r"(n - 1 - m) >= "min" $

  其中 $"len_r"(n-1-m) = r_(m+2) + dots + r_n$ 是剩余圆半径之和的松弛下界，若此下界 ≥ 当前最优宽度则剪枝。

#figure(
  grid(
    columns: (1fr, 1fr),
  image("../assets/ch9-circle-pruning-1.png"),
  image("../assets/ch9-circle-pruning-2.png")
  ),
)
算法在 *排列树*（Permutation Tree）上进行深度优先搜索（DFS）或值优先检索（LC-检索），并在每一层级配合三种剪枝策略来大幅缩小搜索空间。

#algorithm[
1. 输入 $n$ 个圆的半径数组 $r[1..n]$。
2. 初始化全局当前最小宽度 $min = infinity$。
3. 开启回溯搜索，从第 $1$ 层（即准备放置第 1 个圆）开始向下递归。

回溯与剪枝核心（假设当前处于第 $m$ 层，准备放置第 $m$ 个圆）
- *步骤 A：分支选择* \
  从剩余未使用的圆中挑选一个，尝试放入第 $m$ 个位置。
- *步骤 B：相同半径剪枝* \
  若当前挑选的圆，其半径与本层刚刚尝试过并已回溯的某个圆半径完全相同，则直接跳过该分支（因为相同半径的圆互换位置，后续子树完全等价）。
- *步骤 C：镜像对称剪枝* \
  当 $m = 1$ 时，利用排列的整体对称性（如 $(1,2,dots,n)$ 与 $(n,dots,2,1)$ 宽度完全相同），固定第一个圆的挑选范围，仅需计算 $n! / 2$ 种排列。
- *步骤 D：下界约束加强与剪枝* \
  计算出第 $m$ 个圆的坐标 $x_m$ 后，利用下式评估当前状态在最乐观情况下的松弛下界：
  $ "min_len"(m) + 2 sqrt(r_m r_(m+1)) + "len_r"(n - 1 - m) >= "min" $
  其中 `"min_len"(m)` 为前 $m$ 个圆已形成的中间宽度，$"len_r"(n - 1 - m) = r_(m+2) + dots + r_n$ 为剩余圆半径之和。若该下界已大于或等于当前最优解 $min$，则直接 *剪枝并回溯*。
- *步骤 E：向下递归* \
  若成功通过所有剪枝条件，则正式记录状态并递归进入第 $m+1$ 层。

到达叶子节点（$m = n$）
当所有圆全部排入序列后，执行最终的整体宽度计算与最优解更新。
]

#remark[
  圆排列的分支限界实现在排列树上做 LC-检索，配合三种剪枝策略（镜像对称、相同半径、下界约束）大幅缩小搜索空间。最坏复杂度仍为 $O(n!)$，但实际运行中剪枝效果显著。
]
