= chapter2 归纳法
<chapter2-归纳法>
将一个问题分割成多个子问题，这些子问题与原来的问题有相同的结构，分而治之。
== 归纳算法举例
<归纳算法举例>
=== 多项式求值
<多项式求值>
假设有$n + 2$个实数$a_0 \, a_1 \, . . . \, a_n$和$x$的序列，现在要对以下多项式求值：
$ P_n( x) = sum_(i = 0)^n a_i x^i $ 我们发现：
$ p_n( x) =(( . . .((( a_n x + a_(n - 1)) x + a_(n - 2)) x + a_(n - 3)) . . .)) x + a_0 $
所以，我们得到具体表达式为：
$ p_(n - 1)( x) = a_n x^(n - 1) + a_(n - 1) x^(n - 2) +dots+ a_2 x + a_1 $
所以，有递推关系： $ p_j( x) = x p_(j - 1)( x) + a_(n - j) $

#quote[Horner rule是什么?好吧就是上面那个]

=== 生成排列
<生成排列>
#block[
  perm1(1) \
  perm1(m) \
// 不是这个block是干嘛的
]
显然，对于重复次数$f( n)$有以下递推关系：
$ f( n) = n f( n - 1) + n \, n gt.eq 2 $
令$f( n) = n ! h( n)$, 那么
$ n ! h( n) = n( n - 1) ! h( n - 1) + n $ 即
$ h( n) = h( n - 1) + frac(1,( n - 1) !) $ 解得
$ h( n) = h( 1) + sum_(i = 1)^(n - 1) frac(1, i !) <= e - 1 $
所以 $ f( n) = n ! h( n) <( e - 1) n ! < 2 dot.op n ! $
而输出语句的复杂度是$Theta( n)$，所以最终$T( n) = Theta( n dot.op n !)$

这里用到的思想其实是一种累加法的变体。也就是考虑递推式
$ f( n) = g( n) f( n - 1) + h( n) $

只需进行合适的换元，令$p( n)$满足：
$ f( n) =( product_(i = 1)^n g( i)) dot.op p( n) $
这样得到：
$
 ( product_(i = 1)^n g( i)) dot.op p( n) =( product_(i = 1)^n g( i)) dot.op p( n - 1) + h( n)
$
$ p( n) = p( n - 1) + frac(1, product_(i = 1)^n g( i)) h( n) $
$ p( n) = p( 1) + sum_(i = 1)^n frac(1, product_(j = 1)^i g( j)) h( i) $
由此，即可求出$f( n)$，不过要注意无穷级数的作用。

// #remark["重生之我是C++STL大神之std::next_permutation"]
  “寻找下一个排列”的接口：std::next_permutation
C++ <algorithm> 头文件中提供了 std::next_permutation(begin, end)。

功能：将序列重排为字典序中的下一个排列。如果当前已经是最大排列（降序），则重排为最小排列（升序）并返回 false，否则返回 true。

时间复杂度： O(N) ，其中 N 是元素个数。平均情况下非常快（均摊常数时间）。
实现原理（字典序算法）：
它不需要知道总共有多少个排列，也不需要计算阶乘。它只通过比较相邻元素的大小来操作。

算法步骤（以 [1, 3, 2] 为例）：
从右向左查找第一对满足 a[i] < a[i+1] 的元素。找到分界点 i。
在 [1, 3, 2] 中，1 < 3，所以 i=0 (元素1)。
从右向左查找第一个大于 a[i] 的元素 a[j]。
在 [1, 3, 2] 中，右边比 1 大的有 3 和 2，最右边的是 2。所以 j=2 (元素2)。
交换 a[i] 和 a[j]。
交换 1 和 2 -> [2, 3, 1]。
反转 i+1 到末尾的所有元素。
反转 3, 1 -> [2, 1, 3]。

结果：[2, 1, 3] 就是 [1, 3, 2] 的下一个排列。


=== 基数排序
<基数排序>
令 $L = { a_1 \, a_2 \, dots.h \, a_n }$ 是一张有 $n$
个数的集合，其中每个数恰好有 $k$ 位数字，就是说每个数具有
$d_k d_(k - 1) dots.h d_1$ 形式，这里 $d_i$ 是 $0$ 到 $9$ 中的一个数字。

我们研究一种排序算法对 $L$
进行非降序排列，使算法在几乎所有的实际应用中都以线性时间运行。

在这个问题中，我们对整数的大小 $k$ 施行归纳，而不是对数 $n$
这个对象来用归纳法。

#block[
]
时间复杂度：$Theta( k n)$, 空间复杂度：$Theta( 10 n)$

=== 寻找多数元素
<寻找多数元素>
令 $A \[ 1 . . n \]$ 是一个整数序列，$A$ 中的整数 $a$ 如果在 $A$
中出现的次数多于 $floor.l n \/ 2 floor.r$，那么 $a$ 称为多数元素。

- 初始，令候选元素$c = A \[ 1 \]$，并将计数器置$1$

- 从$A \[ 2 \]$开始，逐个扫描元素，如果被扫描的元素和$c$相等，则计数器加$1$；如果元素不等于$c$，则计数器减$1$

- 如果所有的元素都已经扫描完毕并且计数器大于$0$，那么返回$c$作为多数元素的候选者

- 如果在$c$和$A \[ j \]$（$1 < j < n$）比较时计数器为$0$，那么对于$A \[ j + 1 . . n \]$上的元素递归调用candidate过程。

#block[
  $j arrow.l m$\; $c arrow.l A \[ m \]$\; $c o u n t arrow.l 1$

]
#block[
  $c arrow.l upright("candidate")( 1)$ $c o u n t arrow.l 0$

]
时间复杂度$Theta( n)$，空间复杂度$Theta( 1)$

=== 棋子移动游戏
<棋子移动游戏>
洛谷题：#link("https://www.luogu.com.cn/problem/P1259")

对于最后处理的4对棋子是需要特殊对待的，不然会在2白2空2黑的时候卡死。

== 本章习题
<本章习题>
=== RADIXSORT算法时间复杂性
<radixsort算法时间复杂性>
当输入由下列区间中的$n$个正整数组成时，以$n$为大小说明算法RADIXSORT的时间复杂性。

+ $\[ 1 dots.h n \]$

+ $\[ 1 dots.h n^2 \]$

+ $\[ 1 dots.h 2^n \]$

#figure(
  align(center)[#table(
    columns: 3,
    align: (left, left, left),
    table.header([#strong[输入区间]], [$bold("时间复杂度")$], [#strong[解释]]),
    table.hline(),
    [$\[ 1 dots.h n \]$],
    [$O( n)$],
    [最大位数$d = ceil.l log_10 n ceil.r approx O( 1)$，视为常数，故线性时间。],
    [$\[ 1 dots.h n^2 \]$],
    [$O( n^2)$],
    [位数$d = ceil.l 2 log_10 n ceil.r$，但数字范围扩大，近似为$d = O( n)$。],
    [$\[ 1 dots.h 2^n \]$],
    [$O( n dot.op 2^n)$],
    [位数$d = ceil.l n log_10 2 ceil.r = Theta( n)$，且输入规模为$2^n$量级。],
  )],
  caption: [基数排序（RADIXSORT）的时间复杂性分析],
  kind: table,
)

=== 寻找名人问题
<寻找名人问题>
在一个房间里有$n$个人，其中一个是名人。所谓名人，就是大家都认识他，但是他却不认识任何其他人。名人之外的其他人可能认识房间里另外的一部分人。你可以问房间里任何人问题，但是问题只能是："你认识某某吗？"，对方回答"Yes"或者"No"。请设计一个时间复杂度为$Theta( n)$的归纳算法，通过问问题，把房间中的名人找出来。

+ #strong[初始化候选人]：假设第一个人为候选名人 `candidate = 0`。

+ #strong[遍历排除非名人]：

+ 从第2个人到第$n$个人，依次与候选人比较：

  - 若候选人认识当前人，则候选人不是名人，更新候选人为当前人。

  - 若候选人不认识当前人，则当前人不是名人，保留候选人。

+ #strong[验证候选人合法性]：

  - 检查是否所有人认识候选人。

  - 检查候选人是否不认识任何人。
