

#let np = $bold("NP")$
#let npc = $bold("NPC")$
#let ltk = $lt.eq.slant_K$
#let p = $bold("P")$
#import "../lib.typ": *
// #show: conf.with(
//   sidebar: false,
// )
#set line(length: 100%, stroke: 0.1pt)
= chapter7: NP完全问题
<chapter7-np完全问题>
两个基本论题:Church-Turing,Cook-Karp.
- Church-Turing论题:一个问题时可计算的当且仅当它在 图灵机上经过有限次计算得到正确的结果.
  
  这一理论框架将人类面临的所有问题划分为可计算与不可计算两大类别，其中不可计算问题（或称不可解问题）具有本质上的局限性------无论投入多少计算时间与资源，都无法通过计算机获得解决方案。值得注意的是，这里的\"有限次计算\"是一个相对宽松的条件，主要强调#strong[计算过程必须具有终止性]。

- Cook-Karp论题:一个问题是实际可计算的当且仅当它在图灵机上经过多项式时间计算得到正确的结果。
  
  这一划分产生了实际可计算问题与难解问题的新分类：前者具有可行的算法解决方案，后者虽然理论上仍可通过计算机求解（如采用指数时间算法），但由于缺乏多项式时间算法而被认为在实际应用中难以处理。这两个论题共同构成了计算复杂性理论的重要基石，为计算机科学中问题可解性的研究提供了理论基础。

可计算的问题需要满足：

+ 必须是存在解的问题。（例如：是否有外星生命就不行）

+ 可以通过计算机求解的问题，可以用一系列机械的数学步骤（算法）来求解。（例如：证明一个整数$n$的阶乘的末尾数只能为0,1,4,5,6,9种的一个（费马小定理），这个问题是有解的，但目前没有找到有效的证明方法，也不属于可计算问题）

+ 一些需要人类创造力、想象力等方面的问题，也不属于可计算问题。

算法的复杂性是指解决特定问题时，具体算法执行所需的时间量度，这反映了算法本身的效率特性。与之相对，问题的复杂性则表征问题本身固有的难度级别，这是问题与生俱来的本质属性。

从根本上看，问题的复杂度是问题自身内在的、不可改变的特性；而算法的复杂度则受到多重现实条件的制约，既包括逻辑层面的算法设计能力，也包含物理层面的实现条件限制。这种区分揭示了计算理论中问题本质属性与解决方案特性之间的重要分野。

求解⼀个问题的好的算法的时复杂度函数应该恰好等于问题的计算复杂度。

算法最优性指的是在解决特定问题的所有可能算法中，效率最高的算法。这种最优性可以从两个重要维度进行衡量：最坏情况下最优和平均情况下最优。

#strong[最坏情况下最优算法]是指在解决某问题的所有算法中，不存在其他算法在最坏情况下的时间复杂性比该算法更低。这意味着该算法在最不利输入情况下仍能保持最佳性能。

#strong[平均情况下最优算法]则是指在解决某问题的所有算法中，不存在其他算法在平均情况下的时间复杂性比该算法更低，这表明该算法在典型输入情况下具有最优表现。这两种最优性标准为算法性能评估提供了重要理论框架。

== 计算复杂度的分析
<计算复杂度的分析>
=== 平凡下界
<平凡下界>
有的问题必须扫描完所有输入才能得到解，算法总要得到所有输入，输出才能解决好，因此算法的输入规模/输出规模是它的平凡下界。

=== 计数所需最少运算数
<计数所需最少运算数>
找最大问题：在$n$个不同的数中找最大的元素，基本元素是元素的比较，则对于任何算法在最坏情况下至少要做$n - 1$次比较。

=== 决策树(Decision Tree)
<决策树decision-tree>
决策树是一种重要的算法分析工具，它以二叉树的形式直观地展现了算法执行过程。这种特殊的树结构中，每个内部节点都代表着算法执行过程中的一次关键运算操作，比如常见的比较运算；而叶子节点则对应着算法最终输出的结果。值得注意的是，在某些特定问题中，算法的输出结果也可能出现在内部节点上，而不仅限于叶子节点。

当我们分析一个具体算法的决策树时，可以清晰地看到算法处理输入实例的完整流程。算法执行总是从决策树的根节点开始，沿着特定的路径向下遍历。在这个过程中，每个节点都会执行一次基本操作，并根据操作结果（如小于、等于或大于）决定下一步的执行路径。这种执行过程实际上就对应着从根节点到某个叶子节点或内部节点的一条完整路径，完整地记录了算法处理该输入实例的整个过程。

从算法分析的角度来看，决策树为我们提供了深入理解算法行为的窗口。对于给定的算法和固定大小的输入规模$n$，不同的输入实例会导致算法在决策树的不同节点终止。这些终止节点实际上将输入实例进行了分类，每个节点都对应着一类具有相同特征的输入。通过分析决策树中节点的总数，特别是叶子节点的数量，我们可以准确地把握算法需要处理的不同输入类别的数量，这对评估算法的复杂度具有重要意义。决策树的这种特性使其成为算法设计和分析中不可或缺的重要工具。

对于给定的$n$,任何通过比较对$n$个元素排序的算法的决策树的深度至少是$ceil.l log n ! ceil.r$.

$
  log n ! & = sum_(j = 1)^n log j gt.eq integral_1^n log x thin d x = log e integral_1^n ln x thin d x \
          & = log e \( n ln n - n + 1 \) \
          & = n log n - n log e + log e \
          & approx n log n - 1.5 n
$

存在多项式时间算法的问题是易求解的(tractable)，*不存在多项式时间算法的问题是难求解的*(intractable).#note[这也是难解性的定义]

== 判定问题
<判定问题>
判定问题指的是只有“yes”和“no”两个解的问题.

对于判定问题，可以使用布尔值来表示问题的答案，并通过判断输出的是\"Yes\"还是\"No\"来确定问题的解。而对于优化问题，需要确定一个最优解；而对于计数问题需要计算问题实例的数目。#strong[为了便于研究，计算复杂性理论研究中将问题限定为判定问题]。

而其他类的问题也都可以通过适当的变换#strong[转化为判定问题]，且不改变问题的难易程度，从而利用计算复杂性理论进行分析和研究。

+ 寻找多数元素问题(MAJORITY),给定一个含有$n$个元素的数组，寻找并返回数组中的多数元素，如果没有多数元素，则返回none.

  可以转换为，多数元素判定(MAJORITY-D):给定一个含有$n$个元素的数组，问数组中是否存在多数元素。

+ 哈密顿回路搜索(HCS): 给定一个无向图G,
  搜索并返回其中一条哈密顿回路，如果没有哈密顿回路，则返回\"No\".（哈密顿回路，是指经过图中每个顶点恰好一次的回路，如果图G中存在哈密顿回路，则称G为哈密顿图。

  可以转化为，哈密顿图判定问题(HC):给定一个无向图G，判断G是否是哈密顿图。

+ 最长公共子序列问题(LCS)：给定取自相同字母集合的两个序列$X$和$Y$，求$X$和$Y$的最长公共子序列。

  可以转化为，K公共子序列问题(KCS):
  给定取自相同字母集合的两个序列$X$和$Y$以及正整数$K$，问$X$和$Y$是否存在长度不小于$K$的公共子序列。

+ 货郎担问题(TSP):
  给定$n$个城市的集合，城市$i$和城市$j$之间的距离为整数，求一条经过每个城市恰好一次最后回到出发点的最短回路。

  TSP-D:
  给定$n$个城市的集合和一个正整数$K$，城市$i$和城市$j$之间的距离为整数，问是否存在经过每个城市恰好一次最后回到出发点，且长度不超过$K$的回路。

== P 类和 NP 类
<p-类和-np-类>
=== 图灵机模型
<图灵机模型>
#definition("图灵机")[
  一台 k-带图灵机 $MM$ 是一个*三元组* $(Gamma , Q , delta)$ ,其中:
  + 有限符号集 $Gamma$ , s.t. $Gamma supset.eq { 0 , 1 , square , gt.tri }$;

  + 有限状态集 $Q$, s.t. $Q supset.eq { q_(s t a r t) , q_(h a l t) }$;

  + 迁移函数 $delta : Q times Gamma^k arrow.r Q times Gamma^(k-1) times { L , S , R }^k$.

  $MM$的第一条带子是只读的输入带,剩下的都是工作带;最后一条带子是输出带.

]
非确定图灵机 $NN$ 指的是有*两个*迁移函数的图灵机,它可以不确定地使用两者的策略之一.换言之,每一步可以有多个选择.从初始状态出发,其所有的可能路径会像二叉树不断向下延伸(类似《银河系漫游指南》的无尽概率跃迁方程/*你到底在说什么*/),每条路径可能终止,也可能不停机.非确定并不是随机.

#definition("非确定图灵机的接受与拒绝,及其时间定义")[
  1. 设非确定图灵机 $NN$,它的每条*计算路径*(也就是一个$delta_i,i in {0,1}$的序列)试图构造一个存在性证明.如果构造成功,输入 1 并称这个终止格局叫#strong[接受格局];；类似的定义#strong[拒绝格局];.当输入 $x$ 的时候,若 $N$ 有一条计算路径终止于接受格局,称 $NN$ *接受* $x$,并记作 $NN (x) = 1$；类似定义*拒绝*.

  2. 对于每一个输入 $x$,以及每一个“非确定性选择序列”,机器 $NN$ 必须在 $T(|x|)$ 步内停机（到达 $q_h$）.

]
// 图灵机的逻辑结构包括：

// - 一个无限长度的带子，这个带子被分成了一个一个的单元格，每个格子有包含一个来自字母表的符号（含有空白）。

// - 一个读写头，可在纸带上左右移动，并可以读、修改格子上的符号。

// - 一个状态寄存器，用来存储图灵机的当前状态。图灵机的所有可能状态数目是有限的，并且有一个特殊的状态，称为停机状态。

// - 一个指令表（控制单元），根据机器当前所处的状态和带子上当前的符号，指示机器进行特定的操作。比如：擦除或者写入一个符号、向左或者向右移动磁头。并改变状态寄存器的值，令机器进入一个新的状态或保持状态不变。

// 图灵机工作时，大体重复如下过程：

// + 读写头读出当前格子的符号

// + 根据当前状态和读到的符号找到对应的控制指令，并执行以下三个动作：

//   + 读写头在格子上擦除或写入一个数字或符号

//   + 图灵机状态转移到一个新的状态

//   + 读写头向左或向右移动一格

// 上述过程会一直重复，直到机器到达一个终止状态，或者无法继续执行操作为止。图灵机的状态转移方案可以是唯一的，也可以不是唯一的。据此，图灵机模型可以分为：确定性图灵机(Deterministic
// Turing Machines, DTM)和非确定性图灵机(Nondeterministic Turing Machines,
// NTM)

可以用 DTM模型描述的算法，称之为确定性算法。设$A$是求解问题$product$的一个算法，如果在求解问题$product$的一个实例时，在整个执行过程中，每一步都只有一种选择，则称$A$是确定性算法。因此如果对于同样的输入，实例一遍又一遍地执行，它的输出从不改变。如果$A$的时间是关于输入实例大小的多项式时间，则称$A$是多项式时间确定性算法。

可以用NTM模型描述的算法，称之为不确定性算法。
#note[
不确定性算法的非形式化定义：

设$A$是求解问题$product$的一个算法，如果算法$A$以如下猜测+验证的方式工作，称算法$A$为不确定性(nondeterminism)算法：

- 猜测阶段：对问题的输入实例$x$产生一个任意字串$y$，在算法的每次运行，$y$可能不同，因此猜测是以不确定的形式工作。

- 验证阶段：在这个阶段，用一个确定性算法验证两件事：首先验证猜测的$y$是否是合适的形式，若不是，则算法停下并回答no；若是合适形式，则继续检查它是否是$x$的解，如果确实是$x$的解，则停下并回答yes，否则停下并回答no。#footnote[这里ppt上说的是“不回答no”,也就是说,如果不是x的解就什么也不做,不输出no;但是,计算复杂性理论一书里和笔记内容保持一致.参见计算复杂性理论P26第三段]

如果猜测和检查都在输入实例的多项式时间完成，则称$A$为多项式时间不确定性算法。

注意：若$A$输出no，并不意味着不存在一个满足要求的解，也就是说，并不意味着$A$不接受该输入，因为猜测可能不正确。
]

=== P 类问题
<p-类问题>
#definition("时间复杂性类")[
  A decision problem $L ⊆ {0, 1}^*$ is in $bold("TIME")(T(n))$, if there exists a $TT MM$ that accepts L
  and runs in time $c T(n)$ for some $c gt 0$.
]

$bold("TIME")(T(n))$是一个集合：所有能在$T(n)$时间内被图灵机解决的问题的集合.那么,$ upright(bold(P)) = union.big_(c gt.eq 1) bold("TIME") (n^c) $

对于一个判定问题，如果能构造出一个多项式求解算法，则可证明该问题属于P类。显然，所有易解问题都是P类问题。（例如：排序问题、不相交集问题、最短路径问题、2着色问题、2可满足问题）

如果对于任意问题$product in C$,
$product$的补也在$C$中，我们说问题类$C$在补运算下是封闭的。比如2着色问题的补就是不2可着色。P类问题在补运算下是封闭的。

=== NP 类问题
<np-类问题>
定义1（NP类问题的验证定义）：
判定问题类NP由这样的问题$Pi$组成：对于这些问题存在一个确定性算法A，该算法在对$Pi$的一个实例展示一个断言解时，能在多项式时间内验证解的正确性。具体而言，如果断言解导致答案是yes，就存在一种方法可以在多项式时间内验证这个解。

定义2（NP类问题的非确定性计算定义）：
$ upright(bold(N P)) = union.big_(c gt.eq 1) bold("NTIME") (n^c) $

定义3:Recall that 
#definition[
  $L subset.eq {0,1}^star, L in "NP", <==> exists p :NN |-> NN, MM$是多项式时间的*图灵机*,且$forall x in {0,1}^star,$ 下述等价关系成立：$x in L <==> exists u in {0,1}^(p(abs(x))).MM(x, u) = 1$


  我们称$MM$是L的*验证器*.若$abs(u) = abs(p(x))$,称$u$是$x$的*证书*.

]
NP 类问题是否对补运算封闭，即$N P ? = overline(bold(N P))$尚未可知。

P类问题是易求解的，NP类问题是易验证的，显然，$P subset.eq N P$目前仍然无法判断是否有或没有$P = N P$，但是因为P在补运算下封闭，所以有$P subset.eq overline(bold("NP"))$。

== 多项式时间规约和NP完全性
<多项式时间规约和np完全性>


#definition[
  $L,L' subset.eq {0,1}^star.$称$L lt.eq.slant_K L'$,如果有一个多项式时间可计算的函数$r:{0,1}^star |-> {0,1}^star$,使得对于所有$x in {0,1}^star$,$x in L <==> r(x) in L'$.

  称$L$是$L'$的*Karp规约*.
]
#definition[
  $L$是*NP-难*的,$forall A in bold("NP")$,使得$A lt.eq.slant_K L$.进一步,如果$L in bold("NP")$,则$L$是*NP-完全*的.
]
#note[
  从直观上看，这种归约关系揭示了两个问题之间的复杂度比较：若A可归约为B，则表明B的时间复杂度至少与A相当，或者说B的求解难度不低于A。换句话说，问题A的难度不会超过问题B的难度，因为只要能解决B，就必然能解决A。
]

容易证明:
- 如果$L_1 prop_(upright("poly")) L_2$，则$L_2 in #p$蕴含$L_1 in #p$；$L_1$是难解的蕴含$L_2$是难解的。

- 如果$L_1 prop_(upright("poly")) L_2$，则相对于多项式时间，如果$L_2$是易解的，则$L_1$也是易解的，即$L_2$不会比$L_1$更容易。或者反过来讲，即$L_1$不会比$L_2$更难。

- ：设$L_1$、$L_2$和$Pi_3$是三个判定问题，且有$L_1 prop_(upright("poly")) L_2$，$L_2 prop_(upright("poly")) Pi_3$，则$L_1 prop_(upright("poly")) Pi_3$。


== Cook-levin定理
SAT是可满足的合取范式,也就是$and.big(or.big v_(i j))$.Cook-levin定理就是想说SAT是*NP-完全*的.我们完全可以不关心证明,因为这里太小写不下.
== NPI
#image("/assets/image-4.png")
NPI class: the class of problems that they and their  complementation are in NP, but are not NP-complete

== NPC相关证明问题
=== 预备知识
- TSP问题:给定n个城市和它们之间的距离矩阵，以及距离L，是否存在从某个城市出发、经过每个城市一次且仅一次、最后回到出发城市且总距离 ≤ L 的路线
- 顶点覆盖：给出一个无向图$G = \( V \, E \)$和一个正整数$k$，是否存在大小为$k$的子集$C subset.eq V$，使$E$中的每条边至少和$C$中的一个顶点关联？

- 独立集：给出一个无向图$G = \( V \, E \)$和一个正整数$k$，是否存在$k$个顶点的子集$S subset.eq V$，使得对于每一对顶点$u \, w in S$，$\( u \, w \) in.not E$？

- 团集：给出一个无向图$G = \( V \, E \)$和一个正整数$k$，$G$包含一个大小为$k$的团集吗？（注意一个$G$中大小为$k$的团集是$G$中$k$个顶点的一个完全子图。）
- 集合覆盖:给定全集U及其子集构成的集合S，是否存在一个S的子集C，使得C的阶不超过k且C中集合的并集等于U？
  

=== $"TSP" in #npc$.
#block[
  #emph[Proof.] (1) $"TSP" in #np$.对无向完全图$G$和正整数$K$,猜测$G$中所有顶点的一个排列(代表可能的路线),然后检查权值和.

  \(2) $"HC" lt.eq_(K) "TSP"$.对于一个无向无权图$G = angle.l V,E angle.r$,构造无向带权的完全图$G' = angle.l V,E' angle.r$,且权值$d(v_1,v_2) = cases(delim: "{", L\/n \, & upright("若 ") \( u \, v \) in E, L+1 \, & upright("若 ") \( u \, v \) in.not E)$.若$G$有哈密顿回路,$G'$
也有,而且权值为$n times L/n = L$.反之,若$G'$的TSP问题有解,那肯定边都落在$E$里,进而$G$是一个哈密顿环路的图.
]

=== $"3SAT"#ltk "SAT" $.
#block[
  #emph[Proof.] 
    考虑$ z_1 <==> \( z_1 or y_(i 1) or y_(i 2) \) and \( z_1 or not y_(i 1) or y_(i 2) \) and \( z_1 or y_(i 1) or not y_(i 2) \) and \( z_1 or not y_(i 1) or not y_(i 2) \) $
    $
    z_1 or z_2 <==>\( z_1 or z_2 or y_i \) and \( z_1 or z_2 or not y_i \)
    $

    $
    z_1 or dots or z_k <==>
    \( z_1 or z_2 or y_(i 1) \) and \( not y_(i 1) or z_3 or y_(i 2) \)  and dots.h.c and \( not y_(i \( k - 4 \)) or z_(k - 2) or y_(i \( k - 3 \)) \) and \( not y_(i \( k - 3 \)) or z_(k - 1) or z_k \)
    $

    反之也成立(这是因为3-SAT是SAT的子集).所以,3SAT和SAT是一样困难的.
]

#unim[
  证明$Pi$包含一个已知为NPC问题$Pi'$作为它的一种特殊情况。

最大可满足性（Max-SAT）问题：任给关于$n$个变元$x_1$，$x_2$，$dots.h.c$，$x_n$的$m$个简单析取式$C_1$，$C_2$，$dots.h.c$，$C_m$以及正整数$K$，问是否存在关于这些变元的赋值$t$使得$C_1$，$C_2$，$dots.h.c$，$C_m$中至少有$K$个为真？

对比Max-SAT问题和SAT问题，很容易看出SAT是Max-SAT问题限定$K = m$的特殊情况。

当$K = m$时，赋值$t$使$C_1$，$C_2$，$dots.h.c$，$C_m$中至少有$K$个为真就是使$C_1$，$C_2$，$dots.h.c$，$C_m$全部为真，这当且仅当赋值$t$使$F = C_1 and C_2 and dots.h.c and C_m$为真。

因此当$K = m$时，Max-SAT问题就退化为SAT问题。即SAT可以看作是Max-SAT的子问题。
]




=== 顶点覆盖,独立集,团
Clique $lt.slant_K$ Vertex Cover.$G = angle.l V,E angle.r$有一个大小为$k$的团$S$，补图就有一个大小为$k$的独立集$S$(一组点两两之间都没有边)，则$overline(G)$的所有边全部落在$V\/ S$之中。
#line()
SAT $lt.slant_K$ Clique. $forall S in "SAT"$,S有m个子句,n个元素($x,not x$视为不同的元素).
    $ G = angle.l V,E angle.r,V eq.delta {x_1, x_2, ... , x_n| x_i in S},E eq.delta {(x_i, x_j) | x_i,x_j "不在一个子句中且不矛盾"} $

    如果这 $m$ 个点两两都有边（即"团"），就意味着：
    - 它们来自 $m$ 个不同的子句（涵盖了所有子句）。
    - 它们彼此不冲突、不相关（这些文字可以同时为 True，而不会导致逻辑上的自相矛盾）。#footnote[gemini认为,同一个变量在不同子句中的出现”视为完全独立的顶点]
#line()
SAT $lt.slant_K$ Unique Set. $forall S in "SAT"$,S有m个子句,n个元素($x,not x$视为不同的元素).
    $ G = angle.l V,E angle.r,V eq.delta {x_1, x_2, ... , x_n| x_i in S},E eq.delta {(x_i, x_j) | x_i,x_j "在一个子句中或矛盾"} $
其实目的是构造一个团的补图.如果一个图有团,那它的补图里,团就变成了独立集.
#line()
Vertex Cover $lt.slant_K$ Set Covering.$forall G = angle.l V,E angle.r in "Vertex Cover"$，$cal(U) = E,S_v eq.delta {e in cal(U) | v in e}$，即直接选择Vertex Cover问题中那些能覆盖所有边的点即可。#line()
=== $"2SAT" in #p$
_Proof_.对于每个变量$x$,创建图的两个点$x,not x$.对于字句$x or y$,连边$not x -> y , not y -> x$.

利用Tarjan算法找到强连通分量,也就是任意两个顶点相互可达的最大子图.若$x,not x$在一个分量里,则return false;

else return true.

#line()
Tarjan算法:```cpp
全局变量：
    timer = 0  // 时间戳计数器,stack = []                   // 辅助栈
    on_stack = {}                // 记录节点是否在栈中
    disc[] = 未初始化            // 发现时间
    low[]  = 未初始化            // 最低可达时间
    result = []                  // 存放所有 SCC 的结果

函数 TARJAN(图 G):
    for 图中每个节点 u:
        if disc[u] 未初始化:
            DFS(u)

函数 DFS(u):
    disc[u] = low[u] = timer++
    stack.push(u)
    on_stack[u] = true
    for u 的每条出边 (u → v):
        if disc[v] 未初始化:          // v 是未访问的节点
            DFS(v)
            low[u] = min(low[u], low[v])
        else if on_stack[v]:          // v 在栈中，是祖先
            low[u] = min(low[u], disc[v])
    // 回溯时检查 u 是否是 SCC 的根
    if low[u] == disc[u]:
        scc = []
        loop:
            w = stack.pop()
            on_stack[w] = false
            scc.append(w)
            if w == u: break
        result.append(scc)
```

=== 2着色是P问题
好像还蛮显然,因为对称性,从随便一个点出发然后BFS.如果BFS之间出现冲突,例如一个顶点与它的相邻点有同种染色就return false.
#block[
  #strong[Algorithm TWO-COLORING($G$)]

  $ q u e u e arrow.l upright("空队列")$ 任选顶点 $s in V $
  $ c o l o r \[ s \] arrow.l upright("红色") $
  $ q u e u e . upright("enqueue") \( s \) $

  #strong[return] $c o l o r$

]
=== 规约问题的传递性
  对判定问题$L_1$和$L_2$，证明如下结论：如果$L_1 #ltk L_2$，则$L_2 in #p$蕴含$L_1 in #p$；$L_1$是难解的蕴含$L_2$是难解的。


  #emph[Proof.]

  #strong[(1).]已知$L_1 #ltk L_2$,$L_2 in #p$,即存在多项式时间可计算函数$f$，使得
    $ forall x in Sigma^(*) \, x in L_1 arrow.l.r.double f \( x \) in L_2 $
,存在多项式时间算法$f_2$判定$L_2$.
构造$L_1$的判定算法$f_1$:$forall x in L_1,f_1(x) eq.delta f(f_2(x))$.(牢记规约的定义是等价,也就是可以推回去)

  #strong[(2).]原命题的逆否命题为：若$L_2 in.not upright("难解类")$，则$L_1 in.not upright("难解类")$（即$L_2 in #p$）,考虑第一问即可知道$L_1 in #p$，故$L_1$不是难解的

#note[
  特别地，当\"难解类\"取$P$的补集时：
    $ L_2 in.not overline(#p) arrow.r.double L_1 in.not overline(#p) $
    即$L_2 in #p arrow.r.double L_1 in #p$ 

    我不知道证明的这一段是何意味,但是hhy这么写了,还是不删吧
]

=== 计算复杂度

设平面图$G = \( V \, E \)$，$E$中的边$\( i \, j \)$的权值为两个点$i$和$j$之间的距离。$\|V\|= n$。计算$G$的一棵最小生成树。证明，求解该问题的算法类的时间复杂度下界为$Omega \( n log n \)$。

#block[
  #emph[Proof.] 核心思路是*将排序问题规约到最小生成树问题*.给定待排序的$n$个实数${ x_1 \, x_2 \, dots.h \, x_n }$，构造平面*完全*图$G = \( V \, E \)$:$V = { v_1 \, dots.h \, v_n }$，$E = { \( v_i \, v_j \) divides forall i eq.not j }$,权值$w \( v_i \, v_j \) =\|x_i - x_j\|$

利用Kruskal来求解MST问题:按照边排序、从小到大通过并查集选合法的$n-1$条边,一定可以得到MST.那么求解排序问题就求解了MST问题.

这个规约复杂度是$O(n^2) in "Poly"$,所以命题得证.

  $ upright("MST问题的复杂度下界") gt.eq upright("排序问题的下界") = Omega \( n log n \) $
]
=== 元素唯一性问题
<元素唯一性问题>
设$S$是由$n$个数构成的数组，判断$S$中的元素是否唯一。如果唯一，输出\"Yes\"；否则输出\"No\"。

证明该判定问题的计算复杂度是$Theta \( n log n \)$。*这道题证明的是基于比较的计算模型下的复杂度下界,不能开桶*.

_Proof_.这也是规约.下面着重证明为什么排序问题下界是$n log n$

给定$ W = { \( x_1 \, x_2 \, dots.h \, x_n \) in bb(R)^n divides x_i eq.not x_j \, med forall i eq.not j } $
需先证明$W$可分解为$n !$个互不相交的连通分量$W_pi$.有了$n!$,决策树才能有$ceil.l log n! ceil.r approx n log n - 1.5n$深度.

对每个排列$pi in S_n$（$S_n$为$n$阶对称群），定义：
$ W_pi = { \( x_1 \, dots.h \, x_n \) divides x_(pi \( 1 \)) < x_(pi \( 2 \)) < dots.h.c < x_(pi \( n \)) } $

每个$W_pi$是连通的，由线性不等式组定义的解空间是凸的,而且任意两点可通过直线路径连接：$forall upright(bold(a)) \, upright(bold(b)) in W_pi$，有$t upright(bold(a)) + \( 1 - t \) upright(bold(b)) in W_pi$（$0 lt.eq t lt.eq 1$）

熟知对任意两个不同排列$pi eq.not sigma$，必有：
$ W_pi sect W_sigma = nothing $ ,$forall upright(bold(x)) in W$，可通过排序得到唯一排列$pi$使$x_(pi \( 1 \)) < dots.h.c < x_(pi \( n \))$.故$upright(bold(x)) in W_pi$且不属于其他$W_sigma$

所以在$bb(R)^n$空间中,每个超平面$x_i = x_j$将空间分成两个半空间,一共有$abs(S(n))$个联通分支数量.







=== 杂题

- 0-1 背包问题的动态规划算法是一个多项式时间算法吗？解释你的答案。

  0-1背包问题的动态规划算法不是一个多项式时间算法。该算法的运行时间为$O \( n W \)$，其中$n$是商品数量，$W$是背包能容纳的最大商品总重量。$W$的二进制编码长度为$l = ceil.l lg W ceil.r$，该算法的运行时间为$O \( n 2^l \)$，随着$l$的增加，$O \( n 2^l \)$呈指数级增长。我们通常称0-1背包问题为一个伪多项式时间算法.

- *子例程时间影响*.证明：若一个算法对多项式时间子例程的调用次数为常数，并且每次执行的额外工作量也需要多项式时间，则该算法的运行需要多项式时间。若多项式时间子程序的调用次数修改为多项式，则该算法的的运行可能需要指数时间。

  第一个命题熟知.然后证明第二个命题，举例如下，假设开始输入规模为1，每次迭代输出规模为输入规模的两倍，$n$次迭代后输出规模为$2^n$，输出规模与运行时间多项式相关，该算法的运行时间为指数时间。证明完毕。
