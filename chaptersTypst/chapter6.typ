#import "../lib.typ": *
// #show: conf

= chapter6: 图的遍历
<chapter6-图的遍历>
== DFS与BFS
<DFS与BFS>

#algorithm[DFS][
  ```python
  visited = [False] * n
  def dfs(u):
    visited[u] = True
    for v in graph[u]:
      if not visited[v]:
        dfs(v)
  ```
]

#algorithm[BFS][
  ```python
  visited = [False] * n
  def bfs(start):
    queue = queue([start])
    visited[start] = True
    while queue:
      u = queue.pop()
      for v in graph[u]:
        if not visited[v]:
          visited[v] = True
          queue.append(v)
  ```
]

DFS与BFS的想必大家都不陌生，图上DFS与图上BFS的执行过程也很简明，在此不再赘述。

#unim[
  DFS的特点是单线深入，因而可依照各节点被访问的顺序（dfs序）构建出DFS树，把图中的所有边分为树边和回边。基于这种特点，我们可以构建出DFS序这一概念：$"dfn"[u]$表示节点$u$被访问的时间戳，`"low"[u]`表示在DFS过程中能够通过回边访问到的最早的时间戳。通过比较$"dfn"[u]$和$"low"[v]$（其中$v$是$u$的一个子节点），我们可以判断是否存在割点、桥等重要结构。
  
  DFS是Tarjan等算法的基础。

  BFS的特点是多线并行，因而适用于查找图上的最短路径——当一个节点被访问时，它的所有邻居节点都会被塞入将要访问的队列，这样就保证了第一次访问到某个节点时所经过的路径是最短的。基于这种特点，我们可以构建出无权图（每两条边只有联通或不联通两种状态）上的朴素最短路算法：当前节点为$u$，当前邻居节点为$v$，则 $"dist"[v] = "dist"[u] + 1$，时间复杂度为 $O(V + E)$。
  
  BFS是A\*等算法的基础。
]

== tarjan算法
<tarjan算法>

Robert Tarjan 于 1972 年提出了一种基于深度优先搜索的统一框架，可在 $O(V + E)$ 时间内求解有向图的强连通分量（SCC）、无向图的割点与割边等问题。其核心思想是引入"追溯值"（lowlink），只进行一次 DFS 遍历。

Tarjan 算法在一次 DFS 中为每个顶点维护两个关键值：

- $bold("dfn")[v]$（时间戳 / discovery time）：顶点 $v$ 在 DFS 中被首次访问的次序编号；
- $bold("low")[v]$（追溯值 / lowlink）：在 DFS 树中，从 $v$ 出发经过任意条树边（沿有向边方向），再经过最多一条非树边（返祖边或横叉边，且目标顶点仍在栈中），所能到达的仍在栈中的顶点的最小 $"dfn"$ 值。


#algorithm[tarjan][

  + 初始化：$"index" arrow.l 0$，$S arrow.l "空栈"$
  + 对于每个 $v in V$，若 $v$ 未被访问，调用 $bold("strongconnect")(v)$

  过程 $bold("strongconnect")(v)$：
  1. $"dfn"[v] arrow.l "low"[v] arrow.l "index"$；$"index" arrow.l "index" + 1$
  2. 将 $v$ 压入栈 $S$；标记 $v$ 在栈中
  3. 对于每条边 $(v, w) in E$：
     - 若 $w$ 未被访问（树边）：\ 
       调用$bold("strongconnect")(w)$\ 
       $"low"[v] arrow.l min("low"[v], "low"[w])$
     - 否则若#highlight[满足条件]：\ 
       $"low"[v] arrow.l min("low"[v], "dfn"[w])$
]

这里的“满足条件”依赖于具体问题：
- 对于强连通分量/缩点问题，条件是 $w$ 在栈中（即 $w$ 还未确定所属 SCC），因为只有栈中顶点才可能与 $v$ 属于同一 SCC。
- 对于割点/割边问题，条件是 $w$ 已被访问且 $w$ 不是 $v$ 的父节点（也就是说 $(v, w)$ 是返祖边且不是重边），因为只有返祖边才可能影响 $v$ 的 lowlink 值。

=== 强连通分量
<强连通分量>

#definition[强连通分量][
  在有向图 $G = (V, E)$ 中，若一个顶点子集 $S subset.eq V$ 满足：对任意 $u, v in S$，$u$ 可达 $v$ 且 $v$ 可达 $u$（相互可达），则称 $S$ 为 $G$ 的一个强连通分量（Strongly Connected Component, SCC）。若 $S$ 是极大的（即无法再加入任何顶点而保持强连通性），则称 $S$ 为极大强连通分量。
]

将每个 SCC 缩为一个点后得到的新图称为缩点图（condensation graph），它是一个 DAG（有向无环图），且保留了原图的"宏观"可达关系。

在 tarjan 算法框架的基础上，算法维护一个栈 $S$，存储当前已被访问但尚未确定所属 SCC 的顶点。栈中顶点按 $"dfn"$ 递增（即访问顺序）排列，且同一 SCC 的顶点在栈中连续。在 DFS 回溯过程中，当发现 $"dfn"[v] = "low"[v]$ 时，说明 $v$ 是当前 SCC 的根顶点。此时可以将栈中从栈顶到 $v$ 的所有顶点弹出，形成一个完整的 SCC。

=== 缩点
<缩点>

求出所有 SCC 后，缩点（condensation）的过程如下：

+ 为每个 SCC 分配一个编号 $"scc"[v] in {1, 2, dots.h, "color_cnt"}$；
+ 对新图中的每个 SCC 作为顶点，遍历原图的每条边 $(u, v)$：若 $"scc"[u] eq.not "scc"[v]$，则在新图中添加一条从 $"scc"[u]$ 到 $"scc"[v]$ 的有向边；
+ 新图必为 DAG，可按拓扑序进行后续处理（如 DP）。

缩点常用于求解有向图上的可达性问题、2-SAT 问题的可行解构造等。


#remark[Kosaraju算法][
  Kosaraju 算法是另一种求解有向图强连通分量的经典算法，时间复杂度同样为 $O(V + E)$。其核心思想是两次 DFS：
  1. 对原图进行一次 DFS，记录顶点的完成时间（即 DFS 回溯时的顺序），并将顶点按完成时间降序排列；
  2. 对原图的反图（将所有边反向）进行 DFS，按照第一步中记录的顺序访问顶点。每次 DFS 访问到的顶点集合即为一个强连通分量。
  Kosaraju 算法的优点是实现简单，但需要两次 DFS 和额外的图存储，而 Tarjan 算法只需一次 DFS 和一个栈，通常在实际应用中更为高效。
]

=== 割点
<割点>

#definition[割点][
  在无向连通图 $G = (V, E)$ 中，若删除顶点 $v$ 及其关联边后，$G$ 变为不连通（连通分量数增加），则称 $v$ 为 $G$ 的一个关节点或割点（articulation point / cut vertex）。

  如果一个连通图没有割点，则称其为点双连通图。
]

求解割点问题依然使用 tarjan 算法。tarjan 算法中，割点的判定依赖于 DFS 树的结构和 lowlink 值的计算，但这里的 lowlink 更新条件与 SCC 问题中并不完全一致：在这里，$"low"[v]$ 的含义为：从 $v$ 出发，通过若干条树边后最多经过一条返祖边所能到达的顶点的最小 $"dfn"$ 值；SCC 问题中的 lowlink 限定于在当前栈中更新，而割点问题中的 lowlink 则不受栈的限制。

#strong[根节点]（DFS 树的起始顶点）是割点当且仅当它在 DFS 树中有#strong[至少两个]子节点，因为根的各个子树之间除了通过根以外没有其他路径相连；#strong[非根节点] $v$ 是割点当且仅当存在 $v$ 的一个子节点 $w$，使得 $"low"[w] >= "dfn"[v]$。这意味着以 $w$ 为根的子树无法绕过 $v$ 到达 $v$ 的祖先——$w$ 子树与图中其余部分通过 $v$（且必须通过 $v$）相连，删除 $v$ 就会断开。

#unim[
  #strong[直觉解释：]
  - $"low"[w] > "dfn"[v]$：$w$ 的子树连 $v$ 本身都回不到 → 删 $v$ 必然断开；
  - $"low"[w] = "dfn"[v]$：$w$ 子树可以通过某条返祖边回到 $v$，但无法到达 $v$ 的祖先 → 删 $v$ 后，$w$ 子树仍然断开（$v$ 本身被删除了，回到 $v$ 的边也失效了）。
]

在实现过程中，我们可以在 DFS 回溯时直接判断割点条件，并将满足条件的顶点加入结果集合中。最终得到的割点集合即为图中的所有割点。同时要在 DFS 过程中维护一个 $"child"$ 计数器，记录当前顶点在 DFS 树中的子节点数，以便判定根节点是否为割点。

=== 割边
<割边>

#definition[割边][
  在无向连通图 $G = (V, E)$ 中，若删除边 $e$ 后 $G$ 变为不连通，则称 $e$ 为 $G$ 的割边或桥（bridge / cut edge）。

  如果一个连通图没有割边，则称其为边双连通图。
]


割边算法与割点算法几乎完全相同，唯一区别在于判定条件：设 $(u, v)$ 是 DFS 树中的一条边（$u$ 是 $v$ 的父节点），则 $(u, v)$ 是一条割边当且仅当 $"low"[v] > "dfn"[u]$。注意这里是 #strong[严格大于]。若 $"low"[v] = "dfn"[u]$，说明 $v$ 的子树可以通过返祖边回到 $u$ 本身，即存在另一条路径连通 $u$ 和 $v$，故 $(u, v)$ 不是桥。

#figure(
  align(center)[#table(
    columns: 3,
    align: (left, center, center),
    table.header(
      [#strong[概念]],
      [#strong[判定条件]],
      [#strong[关键区别]],
    ),
    table.hline(),
    [割边（桥）], [$"low"[v] > "dfn"[u]$], [严格大于：子节点能回到父节点则边不是桥],
    [割点（非根）], [$"low"[v] >= "dfn"[u]$], [大于等于：子节点回到父节点本身也没用，父节点已被删除],
    [割点（根）], [$"子节点数" >= 2$], [特判：根没有祖先，需特殊处理],
  )],
  caption: [割点与割边判定条件对比],
  kind: table,
)

#unim()[
  #strong[为何有此区别？]

  对于割边 $(u, v)$，若 $"low"[v] = "dfn"[u]$，子树能通过返祖边回到 $u$，说明 $u$ 和 $v$ 之间至少有两条路径（树边 + 环上的另一条路），删除树边后 $v$ 仍能到达 $u$ $==>$ 边不是桥。

  但对于割点 $u$，若 $"low"[v] = "dfn"[u]$，子树回到的是 $u$ 本身。因为 $u$ 已被整体删除，回到 $u$ 的边随之失效，子树与祖先之间彻底断开 $==>$ $u$ 是割点。
]

=== 统一框架

Tarjan 的贡献在于用一个统一的 DFS + lowlink 框架涵盖了这三个看似不同的问题：

#figure(
  align(center)[#table(
    columns: 4,
    align: (left, center, center, center),
    table.header(
      [#strong[问题]],
      [#strong["low" 更新（非树边）]],
      [#strong[判定条件]],
      [#strong[复杂度]],
    ),
    table.hline(),
    [SCC], [$min("low"[v], "dfn"[w])$（用 $"dfn"$）], [$"low"[v] = "dfn"[v]$], [$O(V + E)$],
    [割点], [$min("low"[v], "dfn"[w])$（用 $"dfn"$）], [$"low"[w] >= "dfn"[v]$（非根）], [$O(V + E)$],
    [割边], [$min("low"[v], "dfn"[w])$（用 $"dfn"$）], [$"low"[w] > "dfn"[v]$], [$O(V + E)$],
  )],
  caption: [Tarjan 算法统一框架],
  kind: table,
)

#unim[
  #strong[几点注意事项：]

  + SCC 问题中的图是有向图，DFS 中除了树边和返祖边外还有横叉边；割点和割边问题中的图是无向图，DFS 中仅有树边和返祖边。

  + SCC 问题中判定的是"当前顶点是否是某个 SCC 的根"（$v$ 自身），而割点/割边问题中判定的是"父-子关系的边/点是否构成割"。

  + 割边判定中 $>$ 与割点判定中 $>=$ 的区别在数学上恰好对应了图的双连通性与点双连通性的差异——一个图可以是边双连通而非点双连通的（存在割点但无桥）。

  + 以上所有问题的时间复杂度均为 $O(V + E)$，空间复杂度 $O(V)$。Tarjan 算法是这些问题的渐进最优算法。
]

== A\*算法
<a-star算法>

A\*（读作"A-star"）算法是一种经典的启发式搜索算法，属于图遍历和最佳优先搜索（Best-First Search）算法家族，亦是 BFS 与 Dijkstra 算法的改进。

=== 核心思想

BFS 使用队列进行逐层扩展，Dijkstra 算法按 $g(n)$（起点到当前节点的实际代价）从小到大扩展。A\* 在此基础之上引入了启发式信息：不仅考虑已走过的代价，还"预测"到目标的剩余代价。

#remark[
  #strong[A\* 的估价函数：]
  $ f(n) = g(n) + h(n) $

  - $g(n)$：从起点 $S$ 到当前节点 $n$ 的实际路径代价（已知）；
  - $h(n)$：从当前节点 $n$ 到目标节点 $G$ 的估计代价（启发函数），$h(n) gt.eq 0$；
  - $f(n)$：从起点 $S$ 出发，途经节点 $n$ 到达目标 $G$ 的估计总代价。
]

A\* 在搜索的每一步，都从候选节点集合（OPEN 表）中选择 $f(n)$ 值最小的节点进行扩展。由于 $h(n)$ 携带了问题自身的启发性信息（如地图上的直线距离），A\* 能够"更有方向感"地朝目标推进，避免了盲目搜索的大量无效扩展。

A\* 算法统一了 BFS、Dijkstra（代价树的广度优先搜索）和贪心最佳优先搜索，取不同的 $f(n)$ 即可退化到对应算法：

#figure(
  align(center)[#table(
    columns: 4,
    align: (left, center, center, center),
    table.header(
      [#strong[算法]],
      [#strong[估价函数]],
      [#strong[$h(n)$]],
      [#strong[最优性]],
    ),
    table.hline(),
    [BFS], [$f(n) = d(n)$], [不使用（无权图）], [是（无权图）],
    [Dijkstra / UCS], [$f(n) = g(n)$], [$h(n) = 0$], [是（非负权）],
    [贪心最佳优先], [$f(n) = h(n)$], [任意启发函数], [否],
    [A\*], [$f(n) = g(n) + h(n)$], [可纳启发函数], [是（$h$ 可纳时）],
  )],
  caption: [A\* 与其他搜索算法的关系],
  kind: table,
)

其中 $d(n)$ 为节点 $n$ 在 BFS 搜索树中的深度（从起点经过的边数）。

- 若 $h(n) = 0$（可纳的平凡启发函数），A\* 退化为 Dijkstra / UCS；
- 若 $g(n) = 0$（忽略已付出代价），A\* 退化为贪心最佳优先搜索；
- 若所有边权为 $1$ 且 $h(n) = 0$，A\* 退化为 BFS。

=== 启发函数

#definition[可纳启发函数][
  启发函数 $h(n)$ 称为可纳的（admissible），若对任意节点 $n$，有：
  $ 0 lt.eq h(n) lt.eq h^* (n) $

  其中 $h^* (n)$ 为从 $n$ 到目标的真实最短距离。即 $h(n)$ 永不#strong[高估]实际剩余代价——它总是"乐观"的。
]


#strong[定理（A\* 树搜索的最优性）.] 若 $h$ 可纳，则 A\* 的树搜索版本（不维护 CLOSED 表）找到的解一定是最优的。

常见可纳启发函数举例：
- 网格地图上的曼哈顿距离（四方向移动）；
- 欧几里得平面上的直线距离；
- $h(n) = 0$（退化为 Dijkstra，平凡可纳）。

#definition[一致启发函数][
  启发函数 $h(n)$ 称为一致的（consistent）或单调的（monotone），若对任意节点 $n$ 及其后继 $n'$，满足三角不等式：
  $ h(n) lt.eq c(n, n') + h(n') $

  其中 $c(n, n')$ 为从 $n$ 到 $n'$ 的边代价。
]

  #strong[一致性 $arrow.r.double$ 可纳性.] 一致的启发函数必定是可纳的，反之不一定成立。一致性是可纳性的更强版本。

  #strong[定理（A\* 图搜索的最优性）.] 若 $h$ 一致，则 A\* 的图搜索版本（维护 CLOSED 表）找到的解一定是最优的，且 $f$ 值沿任何路径非递减——首次扩展节点时即已找到到达该节点的最优路径，无需重新打开 CLOSED 节点。

  在实践中，常用的启发函数（曼哈顿距离、欧几里得距离等）通常既满足可纳性又满足一致性。

#unim[
  #strong[直观理解]：可纳性保证"从上方逼近"真实代价（全局约束），一致性保证"局部不会倒推"（局部三角不等式）。一个可纳但不一致的启发函数可能让 $f$ 值在路径上出现下降，导致算法先走了一条次优路径再"回头"，降低了效率。
]

=== 算法实现

A\* 算法维护两个集合：

- #strong[OPEN 表]：优先队列，存放已生成但尚未扩展的候选节点，按 $f(n)$ 从小到大排序；
- #strong[CLOSED 表]：存放已扩展过的节点，避免重复扩展。

#algorithm[A-star Search][
  输入：带权图 $G$，起点 $S$，目标 $G$（这里用 $T$ 表示以避免与图名混淆），启发函数 $h$

  1. 将 $S$ 放入 OPEN 表，设 $g(S) arrow.l 0$，$f(S) arrow.l h(S)$；CLOSED 表置空
  2. 若 OPEN 表为空，则搜索失败，退出（无解）
  3. 从 OPEN 表中取出 $f$ 值最小的节点，记为 $n$，将其移入 CLOSED 表
  4. 若 $n$ 即为目标节点 $T$，则搜索成功，沿父节点指针回溯得到最优路径，退出
  5. 若 $n$ 不可扩展（无后继节点），则转步骤 2
  6. 扩展节点 $n$，生成其所有后继节点 $n_i$（$i = 1, 2, 3, dots.h$）：
     - 计算 $g(n_i) = g(n) + c(n, n_i)$；
     - 计算 $f(n_i) = g(n_i) + h(n_i)$；
     - 为 $n_i$ 设置指向 $n$ 的父节点指针；
     - 若 $n_i$ 不在 OPEN 表中，将其加入 OPEN 表；
       若 $n_i$ 已在 OPEN 表中且新的 $g(n_i)$ 更小，则更新其 $g$、$f$ 值和父节点指针
  7. 对 OPEN 表中的全部节点按 $f$ 值从小到大重新排序（全局择优），转步骤 2
]

#unim[上述第 6 步中，若启发函数 $h$ 是#strong[一致的]，则一个节点一旦进入 CLOSED 表就不会再被重新打开——因为此时 $f$ 值沿任何路径都是非递减的，首次扩展即已找到最优路径。若 $h$ 仅满足可纳性而非一致性，则可能需要重新打开 CLOSED 表中的节点（当发现更优的 $g$ 值时）。]

=== 复杂度分析

  设图的分支因子（平均每个节点的子节点数）为 $b$，最优解的深度为 $d$。

  - #strong[时间复杂度]：最坏情况下为 $O(b^d)$，即指数级。但在好的启发函数引导下，实际扩展的节点数远小于此上界。若 $h$ 是完美启发（$h = h^*$），A\* 仅扩展最优路径上的节点，复杂度降为 $O(d)$。
  - #strong[空间复杂度]：$O(b^d)$——A\* 需要同时维护 OPEN 和 CLOSED 两个集合，这是其主要瓶颈。在大规模搜索问题中，空间往往先于时间耗尽。

#unim[
  === IDA\*（迭代加深 A\*）
  为了降低 A\* 的空间复杂度，可以采用 IDA\*（Iterative Deepening A\*）算法。IDA\* 不维护 OPEN/CLOSED 表，而是进行一系列带 $f$ 阈值的深度优先搜索，逐步增加阈值，直到找到目标节点。这样，IDA\* 的空间复杂度降为 $O(d)$，但时间复杂度可能会增加，因为节点可能被多次扩展。

  #block[
    1. 初始阈值 $"bound" = h("start")$
    2. 执行 DFS：若节点的 $f = g + h > "bound"$，则剪枝并记录该 $f$ 值
    3. 若本次 DFS 未找到目标，将 $"bound"$ 更新为本轮所有被剪枝节点的最小 $f$ 值
    4. 以新 $"bound"$ 重新开始 DFS，重复直至找到目标或确认无解
  ]

  每次迭代的阈值恰好是上一轮"差一点就够到"的最小 $f$ 值，保证每轮至少多探索一个新节点。

  #algorithm[IDA\*][
  #emph[function] $bold("IDA\*")("root")$：
  1. $"bound" arrow.l h("root")$；$"path" arrow.l ["root"]$
  2. 循环：
     - $t arrow.l bold("SEARCH")("path", 0, "bound")$
     - 若 $t = "FOUND"$：返回 $"path"$
     - 若 $t = oo$：返回失败（无解）
     - $"bound" arrow.l t$（提升阈值）

  #emph[function] $bold("SEARCH")("path", g, "bound")$：
  1. $"node" arrow.l "path"$.last；$f arrow.l g + h("node")$
  2. 若 $f > "bound"$：返回 $f$（剪枝，返回候选阈值）
  3. 若 $"node"$ 是目标：返回 $"FOUND"$
  4. $"min" arrow.l oo$
  5. 对 $"node"$ 的每个后继 $"succ"$ 且 $"succ"$ 不在 $"path"$ 中：
     - $"path"$.push($"succ"$)
     - $t arrow.l bold("SEARCH")("path", g + c("node", "succ"), "bound")$
     - 若 $t = "FOUND"$：返回 $"FOUND"$
     - 若 $t < "min"$：$"min" arrow.l t$
     - $"path"$.pop()
  6. 返回 $"min"$
  ]

  #figure(
    align(center)[#table(
      columns: 3,
      align: (left, center, center),
      table.header([#strong[特性]], [#strong[A\*]], [#strong[IDA\*]]),
      table.hline(),
      [空间], [$O(b^d)$ 指数级], [$O(d)$ 线性],
      [节点重复扩展], [无（CLOSED 表保护）], [有（无记忆跨轮次）],
      [最优性], [$h$ 可纳时保证], [$h$ 可纳时保证],
      [最优效率], [是（扩展节点数最少的同类算法）], [否（可能 $O(N^2)$ 次扩展）],
      [适用场景], [内存充裕的中小规模问题], [大规模 / 内存受限问题（如滑块谜题、规划）],
    )],
    caption: [A\* 与 IDA\* 对比],
    kind: table,
  )
]

== 其他应用
=== 判定图是否有环

进行DFS遍历全图，如果在访问一个节点时发现它已经被访问过了，那么就说明存在环；否则不存在环。

=== 拓扑排序

#definition[拓扑序][在有向无环图（DAG）中，如果对于每一条有向边$(u, v)$，节点$u$在序列中都出现在节点$v$之前，那么这个序列就是一个合法的拓扑序。]

对于有向无环图（DAG），我们可以进行DFS遍历全图，并在访问完一个节点的所有邻居节点后（回溯过程）将该节点加入结果列表中。最终得到的结果列表就是一个合法的拓扑序的逆序。

#unim[不难得出，如果建原图的反图（将每条边的方向反转），则反图的DFS序就是原图的拓扑序。]