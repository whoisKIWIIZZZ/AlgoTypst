## 第 14 章 随机算法

## 14.1 引言

本章讨论算法设计的一种形式。在此形式中，我们将算法必须对所有可能的输入都正确地求解问题的条件放宽，只要求它的可能不正确性解能够安全地忽略掉，比如说它的出现可能性非常低。而且也不要求对于特定的输入，算法的每一次运行的输出都必须相同。我们将关注执行的过程能像抛硬币那样，会产生真正随机结果的那些算法。加入这一随机性元素的后果是令人吃惊的，引入随机性是非常有用的，并且对那些效率很低的确定性算法问题有生成快速的解的能力，而不会产生不可预计的结果。

随机算法可以做如下定义：它是在接收输入的同时，为了随机选择的目的，还接收一串随机比特流并且在运行过程中使用该比特流的算法。一个随机算法在不同的运行中对于相同的输入可以有不同的结果。由此得出对于相同的输入两次不同的随机算法的执行时间可能不同。现在已经认识到，在很大的应用范围内随机化对于算法的构造是一个极其重要的工具。随机算法通常有两个主要优点：首先，较之那些我们所知的解决同一问题最好的确定性算法，随机算法所需的运行时间或空间通常小一些；其次，如果我们看一下迄今为止已经发明的各种随机算法，我们发现这些算法总是无一例外地易于理解和实现。下面是随机算法的一个简单例子。

例 14.1 假定有一个 n 个变量的多项式表达式 $f(x_{1}, x_{2}, \cdots, x_{n})$ ，我们要核查 f 是否恒等于 0。分析这一核查就是一件可怕的工作。假设我们可代之以生成一个随机 n 实向量 $(r_{1}, r_{2}, \cdots, r_{n})$ ，并计算 $f(r_{1}, r_{2}, \cdots, r_{n})$ 的值，如果 $f(r_{1}, r_{2}, \cdots, r_{n}) \neq 0$ ，那么 $f \neq 0$ ；如果 $f(r_{1}, r_{2}, \cdots, r_{n}) = 0$ ，那么或者 f 恒等于 0，或者是我们的 $(r_{1}, r_{2}, \cdots, r_{n})$ 选择非常幸运。如果这样重复几次，继续得到 f = 0，那么就可得出 f 恒等于 0 的结论，而我们出错的可能性就很小。

在某些确定性算法中，特别是对于那些表现出好的平均运行时间的算法，仅是引入随机性就能将一个自然、简单，在最坏情况下表现不好的算法转化成对于每一种可能的输入都有很高可靠性的随机算法。在我们研究14.3节和14.4节的排序和选择的随机算法时，这一优点将会很明显。

## 14.2 Las Vegas 和 Monte Carlo 算法

随机算法可以分成两大类，第一类称为 Las Vegas 算法，它建立的那些随机算法总是或者给出正确的解，或者无解。这和另一类称为 Monte Carlo 算法的随机算法相反，Monte Carlo 算法总是给出解，但是偶尔可能会产生非正确的解。然而，可以通过多次运行原算法，并且满足每次运行时的随机选择都相互独立，使产生非正确解的概率可以减到任意小。

为了能对随机算法的计算复杂性进行一般讨论，首先为评估算法表现引入某些标准是有益的。设 $A$ 是一个算法，如果 $A$ 是确定性的，则算法时间复杂性的一种测度是它的平均运行时间：是指对于每个大小为 $n$ 的输入， $A$ 所取的平均时间。就是说，假设所有的输入呈均匀分布（见1.12.2节）。由于输入的分布可能不是均匀的，这可能是误导。如果 $A$ 是一个随机算法，那么在一个大小为 $n$ 的固定的实例 $I$ 上的运行时间，一次和另一次的执行是可能不相同的。因此，一种更加自然的运行的测度是 $A$ 在固定实例 $I$ 上的期望运行时间，这是用算法 $A$ 反复求解实例 $I$ 的平均时间。于是，说及最坏情况下的期望时间和平均情况下的期望时间是正常的（见练习14.3和练习14.4）。

## 14.3 随机化快速排序

这可能是最为流行的一种随机算法。考虑6.6节中提出的算法QUICKSORT，我们已经证明，假如给出输入元素的所有排列的可能性相同，那么算法的平均运行时间是 $\Theta (n\log n)$ 。但在许多实际应用中并不是这样的，我们也已经反过来证明，如果输入是已经排序的元素，那么运行时间是 $\Theta (n^2)$ ，对于几乎排好序的输入的情况也适用。例如，考虑一种应用，给一个大的已排序文件增添少量元素，然后用算法QUICKSORT来重新排序。在这种情况下，添加的元素越少，运行时间越接近 $\Theta (n^2)$ 。

解决这种问题并保证平均运行时间为 $\Theta(n \log n)$ 的方法是引入预处理步骤，它惟一的目的是改变元素的顺序使之随机排序。这种预处理步骤可在 $\Theta(n)$ 时间运行（见练习14.5）。能够起到同样作用的另一种简单方法是在算法中引入一个随机元素，这可以通过随机地选择拆分元素的主元来完成。随机选择主元的结果放宽了关于输入元素的所有排列的可能性相同的假设。引入这一步来修改原来的算法QUICKSORT，得出下面所示的算法RANDOM-IZEDQUICK-SORT。新算法只是在区间[low…high]中一致随机地选择一个索引 $v$ ，并将 $A[v]$ 和 $A[low]$ 交换，这是因为算法SPLIT用 $A[low]$ 做主元（见6.6.1节）。然后按照原来的算法QUICKSORT继续。这里，函数random(low, high)随机返回一个在low和high之间的数。注意到在low和high之间的任何数都以相等概率 $1/(high - low + 1)$ 生成是很重要的。

## 算法 14.1 RANDOMIZEDQUICKSORT

输入：n 个元素的数组 $A[1\cdots n]$ 。

输出：A 中按非降序排列的元素。

```txt
1. rquicksort(1, n)
过程 rquicksort(low, high)
    if low < high then
        v ← random(low, high)
        互换 A[low] 和 A[v]
        SPLIT(A[low…high], w) {w 是主元的新位置}
        rquicksort(low, w - 1)
        rquicksort(w + 1, high)
    end if
```

现在除了不必再假设输入元素的所有排列有相等的可能性外，算法 RANDOMIZEDQUICKSORT 运行时间的分析几乎和原来算法的分析相同。该算法的运行时间在最坏情况下仍然是 $\Theta(n^{2})$ ，但这与输入的形式无关。如果最坏情况发生，那么是因为用随机数生成器随机选择的主元不凑巧，这个事件发生的概率是非常小的。事实上，没有一种输入的排列可以引起它的最坏情况，算法的期望运行时间是 $\Theta(n \log n)$ 。

## 14.4 随机化的选择算法

考虑6.5节介绍的算法SELECT，我们已经证明了算法的运行时间是 $\Theta (n)$ ，具有一个很大的常系数使得算法变得不切实际，特别是对小的和中等的 $n$ 值也如此。本节中，我们对选择介绍一个既简单又快捷的随机LasVegas选择算法，它的期望运行时间是一个带有小的常系数的 $\Theta (n)$ 。正如算法RANDOMIZEDQUICKSORT一样，在最坏情况下它的运行时间是 $\Theta (n^2)$ ，而且最坏情况的界限也和算法求解的实例无关，它只是一个由随机数发生器选择的一个不凑巧的序列的结果，它发生的概率是非常小的。算法的运作在下述意义上像二分搜索算法一样，不断丢弃输入的一部分直到所求的第 $k$ 个最小元素已经得出。算法的精确描述在RANDOMIZEDSELECT中给出。

算法 14.2 RANDOMIZEDSELECT

输入：n 个元素的数组 $A[1\cdots n]$ 和整数 $k, 1 \leqslant k \leqslant n$ 。

输出：A 中的第 k 小元素。

```txt
1. rselect(A,1,n,k)
过程 rselect(A,low,high,k)
1. v ← random(low,high)
2. x ← A[v]
3. 将 A[low…high] 分成三个数组
    A₁ = {a | a < x}
    A₂ = {a | a = x}
    A₃ = {a | a > x}
4. case
    |A₁| ≥ k: return rselect(A₁,1,|A₁|,k)
    |A₁| + |A₂| ≥ k: return x
    |A₁| + |A₂| < k: return rselect(A₃,1,|A₃|,k - |A₁| - |A₂|)
5. end case
```

下面研究算法 RANDOMIZEDSELECT 的运行时间。不失一般性，假定 A 中元素彼此不同，在最坏情况下，随机数发生器在第 j 次循环时选择第 j 个元素，这样只有一个元素被丢弃，由于把 A 分成 $A_{1}, A_{2}$ 和 $A_{3}$ 所需要的比较次数恰好为 n，所以算法在最坏情况下所做的元素比较的总次数是

$$
n + (n - 1) + \dots + 1 = \frac {n (n + 1)}{2} = \Theta (n ^ {2})
$$

现在用归纳法证明采用这种算法所期望的元素比较次数小于 $4n$ 。设 $C(n)$ 为算法在 $n$ 个元素的序列上执行元素比较的期望的次数，由于 $v$ 是随机选择的，可以假设整数 $1,2,\dots ,n$ 中的任意一个具有相等的概率，我们分 $v < k$ 或 $v > k$ 两种情况进行讨论。如果 $v < k$ ，剩下的元素个数是 $n - v$ ；如果 $v > k$ ，剩下的元素个数是 $v - 1$ 。这样，算法执行的期望元素比较次数是

$$
\begin{array}{r l} C (n) & = n + \frac {1}{n} \Big [ \sum_ {j = 1} ^ {k - 1} C (n - j) + \sum_ {j = k + 1} ^ {n} C (j - 1) \Big ] \\ & = n + \frac {1}{n} \Big [ \sum_ {j = n - k + 1} ^ {n - 1} C (j) + \sum_ {j = k} ^ {n - 1} C j) \Big ] \end{array}
$$

将 k 最大化，产生下面的不等式

$$
\begin{array}{r l} C (n) \leqslant n + \max _ {k} \left[ \frac {1}{n} \left[ \sum_ {j = n - k + 1} ^ {n - 1} C (j) + \sum_ {j = k} ^ {n - 1} C (j) \right] \right] \\ = n + \frac {1}{n} \left[ \max _ {k} \left[ \sum_ {j = n - k + 1} ^ {n - 1} C (j) + \sum_ {j = k} ^ {n - 1} C (j) \right] \right] \end{array}
$$

由于 $C(n)$ 是 $\pmb{n}$ 的非递减函数，因此

$$
\sum_ {j = n - k + 1} ^ {n - 1} C (j) + \sum_ {j = k} ^ {n - 1} C (j)\tag{14.1}
$$

当 $k=\lceil n/2\rceil$ 时最大（见练习 14.6）。因此，由归纳法

$$
\begin{array}{l} C (n) \leqslant n + \frac {1}{n} \left[ \sum_ {j = n - \lceil n / 2 \rceil + 1} ^ {n - 1} 4 j + \sum_ {j = \lceil n / 2 \rceil} ^ {n - 1} 4 j \right] \\ = n + \frac {4}{n} \left[ \sum_ {j = \lceil n / 2 \rceil + 1} ^ {n - 1} j + \sum_ {j = \lceil n / 2 \rceil} ^ {n - 1} j \right] \\ \leqslant n + \frac {4}{n} \left[ \sum_ {j = \lceil n / 2 \rceil} ^ {n - 1} j + \sum_ {j = \lceil n / 2 \rceil} ^ {n - 1} j \right] \\ = n + \frac {8}{n} \sum_ {j = \lceil n / 2 \rceil} ^ {n - 1} j \\ = n + \frac {8}{n} \left[ \sum_ {j = 1} ^ {n - 1} j - \sum_ {j = 1} ^ {\lceil n / 2 \rceil - 1} j \right] \\ = n + \frac {8}{n} \left[ \frac {n (n - 1)}{2} - \frac {\lceil n / 2 \rceil (\lceil n / 2 \rceil - 1)}{2} \right] \\ \leqslant n + \frac {8}{n} \left[ \frac {n (n - 1)}{2} - \frac {(n / 2) (n / 2 - 1)}{2} \right] \\ = 4 n - 2 \\ <   4 n \end{array}
$$

由于每个元素至少被检查一次， $C(n) \geqslant n$ ，于是有下面的定理。

定理 14.1 对大小为 n 的输入，算法 RANDOMIZEDSELECT 执行的期望元素比较次数小于 4n，它的期望运行时间是 $\Theta(n)$ 。

## 14.5 测试串的相等性

本节简要描述一个例子，说明随机化如何能大大减少通信的耗费。假定 $A$ 和 $B$ 两个组能够通过一条信道来通信，并假定信道非常可靠。A 有一个很长的串 x, B 有一个很长的串 y，现在要确定是否 x = y。显然，A 可以把 x 发送给 B，B 可以接着立刻判断是否 x = y。但是从使用信道的代价的角度看，这种方法将非常浪费。另一种方法是 A 从 x 中取出一个短得多的串作为 x 的“指纹”，并把它发送给 B，B 用同样的方法获得 y 的“指纹”，然后比较这两种指纹，如果它们相等，那么 B 就假定 x = y，否则 B 将得出 $x \neq y$ 的结论，之后 B 把测试的结果通报给 A。这种方法只需在信道上传输一个短得多的串。对于一个串 w，设 $I(w)$ 是比特串 w 表示的一个整数，一种收集指纹的方法是选一个素数 p，然后用指纹函数

$$
I _ {p} (x) = I (x) \pmod {p}
$$

如果 $p$ 不是太大的话，则指纹 $I_{p}(x)$ 可以作为一个短串发送，传送的比特数是 $O(\log p)$ 。如果 $I_{p}(x) \neq I_{p}(y)$ ，那么显然 $x \neq y$ 。但逆命题不成立，也就是说，如果 $I_{p}(x) = I_{p}(y)$ ，则不一定有 $x = y$ 的结果。我们称这种现象为假匹配。一般地，如果 $x \neq y$ ，但是 $I_{p}(x) = I_{p}(y)$ ，即 $p$ 整除 $I(x) - I(y)$ 时，就发生了假匹配，我们将在后面界定假匹配概率。

这种方法的弱点在于对固定的 $p$ ，存在一定的串 $x$ 和 $y$ 偶对，对于它们这种方法总是要失败的。我们在每次检查两个串的相等性时随机地选择 $p$ ，而不是预先选好，这样可以避开存在的这些偶对 $x$ 和 $y$ 的问题。而且，随机选择 $p$ 也允许重传另一个指纹，这样在 $x = y$ 的情况增加了可信度。这种方法在下面的算法 STRINGEQUALITYTEST 中给出 ( $M$ 的值在以后确定)。

## 算法 14.3 STRINGEQUALITYTEST

1. $A$ 从小于 $M$ 的素数集中随机选择 $p$ ;

2. $A$ 将 $p$ 和 $I_{p}(x)$ 发送给 $B$ ;

3. $B$ 检查是否 $I_{p}(x) = I_{p}(y)$ ，确定两个串 $x$ 和 $y$ 是否相等。

现在计算假匹配的概率。设 $n$ 是 $x$ 的二进制表示形式的位数，当然， $n$ 也等于 $y$ 的二进制表示形式的位数，否则，问题是平凡的。设 $\pi(n)$ 是小于 $n$ 的不同素数的个数，众所周知， $\pi(n)$ 趋近于 $n / \ln n$ 。我们又知道，如果 $k < 2^n$ ，则除非 $n$ 非常小，否则能够整除 $k$ 的不同质数的个数小于 $\pi(n)$ 。由于失败仅在假匹配的情况下发生，即 $x \neq y$ ，但 $I_p(x) = I_p(y)$ 时，而这只有在 $p$ 整除 $I(x) - I(y)$ 时可能。因此，对于两个 $n$ 位串 $x$ 和 $y$ ，失败的概率是

$$
\frac {| \{p   | \text {素数} p <   2 ^ {n} \text {且} p \text {整除} I (x) - I (y) \} |}{\pi (M)} \leqslant \frac {\pi (n)}{\pi (M)}
$$

如果选择 $M = 2n^{2}$ ，则得到

$$
\mathrm{Pr} [ \text {failure} ] \leqslant \frac {\pi (n)}{\pi (M)} \approx \frac {n / \ln n}{2 n ^ {2} / \ln n ^ {2}} = \frac {1}{n}
$$

更进一步，如果重复执行算法 $k$ 次，每次随机选一个小于 $M$ 的素数，则概率变成最大是 $(1 / n)^{k}$ 。例如，如果设 $k = \lceil \log \log n \rceil$ ，则失败的概率变成

$$
\Pr [ \text {failure} ] \leqslant \frac {1}{n ^ {\lceil \log \log n \rceil}}
$$

例14.2 假定 $x$ 和 $y$ 每个是100万比特串，即 $n = 1000000$ ，则 $M = 2 \times 10^{12} = 2^{40.8631}$ 。在这种情况下，需要传输 $p$ 的比特数最多是 $\lfloor \log M \rfloor + 1 = 40 + 1 = 41$ ，需要传输的 $x$ 的指纹位数最多是 $\lfloor \log (p - 1)\rfloor + 1 \leqslant \lfloor \log M\rfloor + 1 = 41$ 。这样，传输比特总数最多是82。一次传输失败的概率最多是 $1 / n = 1 / 1000000$ 。因为 $\lceil \log \log n \rceil = 5$ ，重复算法5次将假匹配的概率减小到 $n^{-\lceil \log \log n \rceil} = (10^6)^{-5} = 10^{-30}$ ，它是很少的了。

## 14.6 模式匹配

现在把14.5节中描述的收集指纹的同样思想应用到计算机科学中的一类经典问题——模式匹配中去。给出一个文本串 $X = x_{1}x_{2}\dots x_{n}$ 和模式 $Y = y_{1}y_{2}\dots y_{m}$ ，其中 $m\leqslant n$ ，确定该模式是否出现在文本中。不失一般性，我们将假定文本的字母表是 $\Sigma = \{0,1\}$ ，解决这个问题最直接的方法是简单地移动模式经过整个文本，在每个位置将长度为 $m$ 的部分文本和模式做比较，这种蛮力方法在最坏情况下的运行时间是 $O(mn)$ 。然而还有一些更复杂的确定性算法，它们的运行时间是 $O(n + m)$ 。

这里介绍一种简单有效且运行时间也能够达到 $O(n + m)$ 的MonteCarlo算法，之后我们将把它转换成有相同时间复杂性的LasVegas算法。算法按照同样的蛮力算法的方法，把模式 $Y$ 滑过整个文本 $X$ ，但不是将模式与每块 $X(j) = x_{j}x_{j + 1}\dots x_{j + m - 1}$ 做比较，而是把模式的指纹 $I_p(Y)$ 和文本中块的指纹 $I_{p}(X(j))$ 进行比较，幸运的是，文本的 $O(n)$ 个指纹都易于计算。特别值得注意的是，当从文本的一个块移到下一块的时候，新块 $X(j + 1)$ 的指纹可以很方便地从 $X(j)$ 的指纹计算出来。特别是

$$
I _ {p} (X (j + 1)) = (2 I _ {p} (X (j)) - 2 ^ {m} x _ {j} + x _ {j + m}) (\mathrm{mod} p)
$$

如果设 $W_{p} = 2^{m}(\mathrm{mod} p)$ ，则有递归式

$$
I _ {p} (X (j + 1)) = (2 I _ {p} (X (j)) - W _ {p} x _ {j} + x _ {j + m}) (\mathrm{mod} p)\tag{14.2}
$$

模式匹配算法由下面的算法 PATTERNMATCHING 表示（M 的值以后确定）。

算法 14.4 PATTERNMATCHING

输入：长度为 n 的文本串 X 和长度为 m 的模式 Y。

输出：如果 Y 在 X 中，则返回 Y 在 X 中的第一个位置；否则返回 0。

1. 从小于 $M$ 的素数集中随机选择 $p$

2. $j \leftarrow 1$

3. 计算 $W_{p} = 2^{m}(\mathrm{mod} p), I_{p}(Y)$ 和 $I_{p}(X_{j})$

4. while $j \leqslant n - m + 1$

5. if $I_p(X_j) = I_p(Y)$ then return $j$ { (可能)找到了匹配}

6. 用等式14.2计算 $I_{p}(X_{j})$

7. $j \leftarrow j + 1$

8. end while

9. return 0 {Y(肯定)不在 X 中}

每个 $W_{p}, I_{p}(Y)$ 和 $I_{p}(X(1))$ 的计算，耗费 $O(m)$ 时间。在实现从 $I_{p}(X(j))$ 到 $I_{p}(X(j + 1))$ 的计算时，无需采用大耗费的乘除运算，只需常数个数的加减法。这样，对于 $2 \leqslant j \leqslant n - m + 1$ 每个 $I_{p}(X(j))$ 的计算仅需要 $O(1)$ 的时间，总共需要 $O(n)$ 时间。因此，运行时间是 $O(n + m)$ 。上面的分析在一致耗费的计算模型RAM下是有效的。如果采用更实际的对数耗费计算模型RAM，则时间复杂性将增加一个 $\log p$ 因子。

现在分析算法失败的发生频率。一个假匹配的出现，仅当对于某个 j，我们有

$$
Y \neq X (j) \quad \text {但} I _ {p} (  Y) = I _ {p} (  X (j))
$$

这是惟一的可能，如果被选的素数 $p$ 整除

$$
\prod_ {\{j \mid Y \neq X (j) \}} | I (Y) - I (X (j)) |
$$

这个乘积不可能超过 $(2^{m})^{n} = 2^{mn}$ （见14.5节），因此整除它的素数个数不可能超过 $\pi(mn)$ 。如果选择 $M = 2mn^2$ ，则假匹配的概率不可能超过

$$
\frac {\pi (m n)}{\pi (M)} \approx \frac {m n / \ln (m n)}{2 m n ^ {2} / \ln (m n ^ {2})} = \frac {\ln (m n ^ {2})}{2 n \ln (m n)} <   \frac {\ln (m n) ^ {2}}{2 n \ln (m n)} = \frac {1}{n}
$$

有趣的是，我们注意到根据上面的推导，失败的概率只和文本的长度有关，而模式的长度对概率没有影响。还注意到，在 $m = n$ 的情况下，问题简化为14.5节中讨论的测试两个等长串是否相同的问题，失败的概率与从那个问题中得出的结论一致。

将算法转换为 Las Vegas 算法是容易的。只要两个指纹 $I_{p}(Y)$ 和 $I_{p}(X(j))$ 匹配，这两个串就要测试相等性。这个 Las Vegas 算法的期望的时间复杂性变成

$$
O (n + m) \left(1 - \frac {1}{n}\right) + m n \left(\frac {1}{n}\right) = O (n + m)
$$

至此，我们以一个有效的模式匹配算法结束了本节的内容，它总能给出正确结果。

## 14.7 随机取样

考虑这样的问题：从 $n$ 个元素的集合中随机选取 $m$ 个元素的样本，这里 $m < n$ 。为了简单，我们假定元素是1到 $n$ 的正整数。在这一节中，我们介绍一个运行时间为 $\Theta(n)$ 的简单Las Vegas算法。

考虑下面的选择算法，首先将所有的 $n$ 个元素标记为未被选择，然后重复执行下面的步骤直到 $m$ 个元素被选定，生成一个从1到 $n$ 中的随机数 $r$ ，如果 $r$ 标识为未被选择，就将其标识为已选择，并把 $r$ 加入到样本中。这种方法在算法 RANDOMSAMPLING 中有更精确的描述。这种算法的一个缺点是它的空间复杂性是 $\Theta(n)$ ，因为它需要一个数组来标记每个整数。如果 $n$ 和 $m$ 比起来太大的话（比如 $n > m^2$ ），则可以方便地修改算法，使其不需要这个数组（见练习14.16）。

算法 14.5 RANDOMSAMPLING

输入：两个正整数 $m, n, m < n$ 。

输出：从集合 $\{1,2,\cdots,n\}$ 中随机选择的m个不同的正整数组成的数组 $A[1\cdots m]$ 。

1. comment: $S[1 \cdots n]$ 为布尔数组，表示一个整数是否被选择

2. for $i \leftarrow 1$ to $n$

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
3. $S[i] \leftarrow$ false
4. end for
5. $k \leftarrow 0$
6. while $k &lt; m$
7. $r \leftarrow random(1, n)$
8. if not $S[r]$ then
9. $k \leftarrow k + 1$
10. $A[k] \leftarrow r$
11. $S[r] \leftarrow$ true
12. end if
13. end while
</div>

显然， $m$ 和 $n$ 之间的差别越小，运行时间就越长。例如，如果 $n = 1000$ 和 $m = 990$ ，则算法将要花费太多的时间来生成样本中的最后一个整数，例如第990个整数。为了避开这个问题，可以随机选出10个整数，把它们丢弃并把余下的990个整数作为要求的样本。因此，我们将假定 $m \leqslant n / 2$ ，因为否则可以随机选出 $n - m$ 个元素，丢弃它们并将剩余的元素留做样本。

在分析算法的运行时间之前，需要下面的定义。

定义14.1 设 $c$ 是一枚硬币，出现“头像”的概率是 $p$ ，令 $q = 1 - p$ 。假设我们重复地抛硬币 $c$ ，直到“头像”第一次出现。设 $X$ 是一个表示直到“头像”第一次出现之前总的抛掷次数的随机变量，则 $X$ 满足几何概率分布

$$
\mathbf {P r} [   X = k   ] = \left\{ \begin{array}{l l} p q ^ {k - 1} & \text {若} k \geqslant 1 \\ 0 & \text {若} k <   1 \end{array} \right.
$$

$X$ 的期望值是

$$
\mathbf {E} (X) = \sum_ {k = 1} ^ {\infty} k p q ^ {k - 1} = \frac {1}{p}\tag{14.3}
$$

设当已经选定了 $k - 1$ 个整数时， $p_k$ 为生成一个未被选定的整数的概率，其中 $1 \leqslant k \leqslant m$ 。显然

$$
p _ {k} = \frac {n - k + 1}{n}
$$

如果当 $1 \leqslant k \leqslant m$ 时， $X_{k}$ 是一个随机变量，用来表示为了选定第 $k$ 个整数而生成的整数的个数，则 $X_{k}$ 呈几何分布，有期望值

$$
\mathbf {E} (X _ {k}) = \frac {1}{p _ {k}} = \frac {n}{n - k + 1}
$$

设 $Y$ 是表示为了从 $n$ 个整数中选出 $m$ 个数而生成的总的整数个数的随机变量，根据期望的线性，我们有

$$
\mathbf {E} (  Y) = \mathbf {E} (  X _ {1}  ) + \mathbf {E} (  X _ {2}  ) + \dots + \mathbf {E} (  X _ {m}  )
$$

因此

$$
\begin{array}{r l} \mathbf {E} (Y) & = \sum_ {k = 1} ^ {m} \mathbf {E} (X _ {k}) \\ & = \sum_ {k = 1} ^ {m} \frac {n}{n - k + 1} \\ & = n \sum_ {k = 1} ^ {n} \frac {1}{n - k + 1} - n \sum_ {k = m + 1} ^ {n} \frac {1}{n - k + 1} \\ & = n \sum_ {k = 1} ^ {n} \frac {1}{k} - n \sum_ {k = 1} ^ {n - m} \frac {1}{k} \end{array}
$$

根据等式2.16

$$
\sum_ {j = 1} ^ {n} \frac {1}{k} \leqslant \ln n + 1 \text {和} \sum_ {k = 1} ^ {n - m} \frac {1}{k} \geqslant \ln (n - m + 1)
$$

因此

$$
\begin{array}{r l} & {\mathbf {E} (Y) \leqslant n (\ln n + 1 - \ln (n - m + 1))} \\ & {\qquad \approx n (\ln n + 1 - \ln (n - m))} \\ & {\qquad \leqslant n (\ln n + 1 - \ln (n / 2)) \quad (\text {因为} m \leqslant n / 2)} \\ & {\qquad = n (\ln 2 + 1)} \\ & {\qquad = n \ln 2 e} \\ & {\qquad \approx 1. 6 9 n} \end{array}
$$

由于算法的期望运行时间 $T(n)$ 和 $\mathbf{E}(Y)$ 成正比，因此有结果 $T(n)=\Theta(n)$ 。

## 14.8 素数性测试

本节中，我们研究一个著名的用来测试一个正整数 $n$ 是否是素数的MonteCarlo算法。反复地用从2到 $\lfloor \sqrt{n}\rfloor$ 的数去除该整数的方法是显然的，但效率极低，因为它导致了关于输入大小的指数时间复杂性（见例1.16）。这种方法仅仅适用于小的数，惟一的优点在于如果 $n$ 是合数时可以输出 $n$ 的因子，事实上因数分解一个整数与仅仅测试该数是素数还是合数相比，是困难得多的问题。

在素数性测试中，我们采用寻找“证据”的思想，也就是证明一个数是合数。显然，找到 $n$ 的一个因子就是它为合数的证明。但这样的证据非常少。事实上，如果我们取一个相当大的数 $n$ ，它的素因子的个数比从1到 $n$ 之间整数的个数要少很多。已经很清楚，如果 $n < 2^k$ 则除了 $k$ 非常小的时候，能够整除 $n$ 的不同素数的个数小于 $\pi(k) \approx k / \ln k$ 。

这激发我们去探求另一类证据。在讨论另一种证据之前，先对贯穿这一节中用到的一种运算给出算法。设 $a, m$ 和 $n$ 是正整数，且 $m \leqslant n$ ，目的是求 $a$ 的 $m$ 次幂，并将结果模 $n$ ，下面的算法 EXPMOD 计算了 $a^m (\bmod n)$ ，这和 5.4 节介绍的取幂算法类似。注意，算法是在每一次平方或做乘法之后取 $n$ 的模，而不是先计算 $a^m$ 再最后一次性对 $n$ 取模。调用该算法的形式是 EXPMOD $(a, m, n)$ 。

算法14.6 EXPMOD

输入：正整数 $a, m$ 和 $n, m \leqslant n$ 。

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
输出： $a^{m}(\bmod n)$ 。
设 m 的二进制数字为  $b_{k}, b_{k-1}, \cdots, b_{0}$ 
2.  $c \leftarrow 1$ 
3. for  $j \leftarrow k$  downto 0
4.  $c \leftarrow c^{2} (\bmod n)$ 
5. if  $b_{j} = 1$  then  $c \leftarrow ac (\bmod n)$ 
6. end for
7. return c
</div>

容易看到，如果令每次乘法占用一个时间单元，算法 EXPMOD 的运行时间是 $\Theta(\log m) = O(\log n)$ 。然而，因为这里处理的是任意大的整数，我们将计算算法中执行的比特乘法的确切数目。如果用显而易见的两数相乘的方法，则每次乘法耗费 $O(\log^{2} n)$ ，这样，算法 EXPMOD 的全部运行时间是 $O(\log^{3} n)$ 。

现在介绍素数性测试的方法，它们都基于下面的Fermat定理。

定理14.2 如果 $n$ 是素数，那么对于所有的 $a \not\equiv 0 (\bmod n)$ ，有

$$
a ^ {n - 1} \equiv 1 (\mathrm{mod} n)
$$

考虑算法 PTEST1。由 Fermat 定理，如果算法 PTEST1 返回合数，则我们确定 $n$ 是合数。古代中国人猜想：一个自然数 $n$ 若满足同余式 $2^{n} \equiv 2 (\bmod n)$ ，则该数必定是素数。这个问题悬而未决，直到 1819 年，Sarrus 证明出 $2^{340} \equiv 1 (\bmod 341)$ ，但是 $341 = 11 \times 31$ 是合数。一些其他的合数满足同余式 $2^{n-1} \equiv 1 (\bmod n)$ 的，分别是 561, 645, 1105, 1387, 1729 和 1905。这样，如果算法 PTEST1 返回素数，则 $n$ 可能是也可能不是素数。

算法 14.7 PTEST1

输入：正奇整数 $n \geqslant 5$ 。

输出：如果 n 是素数，则返回 prime；否则返回 composite。

```txt
1. if EXPMOD(2, n - 1, n) ≡ 1 (mod n) then return prime {可能}
2. else return composite {确定}
```

令人惊讶的是，这个简单的测试却很少给出错误的结果。例如，对于4到2000之间所有的合数，算法仅对341,561,645,1105,1387,1729和1905这样几个数返回素数。此外，在小于100 000的数中仅有78个值测试出错，它们中最大的是 $93\ 961=7\times31\times433$ 。

不过，事实上对许多合数 $n$ ，存在一个整数 $a$ 满足 $a^{n-1} \equiv 1 (\bmod n)$ 。换句话说，Fermat定理的逆是不正确的（我们已经对 $a = 2$ 的情况做了证明）。的确有被称为Carmichael数的合数 $n$ ，它对于所有相对于 $n$ 互素的正整数 $a$ ，满足Fermat定理。最小的Carmichael数是 $561 = 3 \times 11 \times 17, 1105 = 5 \times 13 \times 17, 1729 = 7 \times 13 \times 19$ 和 $2465 = 5 \times 17 \times 29$ 。Carmichael数相当少，比如在小于 $10^8$ 的范围内仅有255个。当一个合数 $n$ 对于底 $a$ 满足Fermat定理时， $n$ 被称为底 $a$ 的伪素数。于是，算法PTEST1在 $n$ 是素数或者是底2的伪素数时返回素数。

改进算法 PTEST1 的一种方法是在 2 和 n-2 之间随机地选择底，这产生了算法 PTEST2。

像算法 PTEST1 那样，算法 PTEST2 仅当 n 是一个底 a 的伪素数时出现错误。例如， $91 = 7 \times 13$ 是底 3 的伪素数，因为 $3^{90} \equiv 1 (\bmod 91)$ 。

算法14.8 PTEST2

输入：正奇整数 $n \geqslant 5$ 。

输出：如果 $n$ 是素数，则返回prime；否则返回composite。

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
1. $a \leftarrow$ random(2, n - 2)
2. if EXPMOD(a, n - 1, n) ≡ 1 (mod n) then return prime {可能}
3. else return composite {确定}
</div>

设 $Z_{n}^{*}$ 是小于 $n$ 并与 $n$ 互素的正整数集合，已经知道 $Z_{n}^{*}$ 在模 $n$ 乘法运算下构成一个群，定义

$$
F _ {n} = \{a \in Z _ {n} ^ {*} \mid a ^ {n - 1} \equiv 1 (\mathrm{mod} n) \}
$$

如果 $n$ 是素数或Carmichael数，则 $F_{n} = Z_{n}^{*}$ 。因而，假定 $n$ 不是素数或Carmichael数，则 $F_{n}\neq Z_{n}^{*}$ 。容易验证 $F_{n}$ 在模 $n$ 乘法运算下形成一个群，它是 $Z_{n}^{*}$ 的一个真子群。从而 $F_{n}$ 的阶整除 $Z_{n}^{*}$ 的阶，也就是说， $\mid F_n\mid$ 整除 $\mid Z_n^*\mid$ ，这得到 $F_{n}$ 中元素的个数最多是 $Z_{n}^{*}$ 中元素个数的一半。这就证明了下面的引理。

引理 14.1 如果 n 不是 Carmichael 数，则算法 PTEST2 将测出 n 是合数的概率至少是 1/2。

遗憾的是，最近已经证明了实际上有无穷多的 Carmichael 数，在这一节的余下部分，我们描述一个更为强大的随机化素性测试算法，可以绕过存在无穷多的 Carmichael 数带来的困难。算法有如下特性：如果 $n$ 是合数，那么检测到的概率至少是 $1/2$ ，换句话说，出错的概率最多是 $1/2$ 。于是，通过反复测试 $k$ 次，出错的概率最多是 $2^{-k}$ 。基于以下理由我们把该算法称为 PTEST3。设 $n$ 为大于等于 5 的奇素数，写为 $n - 1 = 2^q m$ （由于 $n - 1$ 是偶数，所以 $q \geqslant 1$ ），则由 Fermat 定理，序列

$$
a ^ {m} (\mathrm{mod} n), a ^ {2 m} (\mathrm{mod} n), a ^ {4 m} (\mathrm{mod} n), \dots , a ^ {2 ^ {q _ {m}}} (\mathrm{mod} n)
$$

必定以1结束，而且在第一次出现1之前的值必定是 $n - 1$ ，这是因为当 $n$ 是素数时， $x^{2} \equiv 1 (\bmod n)$ 的惟一解是 $x = \pm 1$ ，因为在这种情况下 $Z_{n}^{*}$ 是一个域。以上推理得出算法PTEST3。

算法 14.9 PTEST3

输入：正奇整数 $n \geqslant 5$ 。

输出：如果 n 是素数，则返回 prime；否则返回 composite。

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
1. $q \leftarrow 0; \quad m \leftarrow n - 1$
2. repeat {查找 $q$ 和 $m$}
3. $m \leftarrow m / 2$
4. $q \leftarrow q + 1$
5. until $m$ 为奇数
6. $a \leftarrow random(2, n - 2)$
7. $x \leftarrow \text{EXPMOD}(a, m, n)$
8. if $x = 1$ then return prime {可能}
</div>

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
9. for $j \leftarrow 0$ to $q - 1$
10. if $x \equiv -1 \pmod{n}$ then return prime {可能}
11. $x \leftarrow x^2 \pmod{n}$
12. end for
13. return composite {确定}
</div>

定理14.3 如果算法PTEST3返回composite，那么 $n$ 是合数。

证明：假设算法 PTEST3 返回 composite，但 n 是一个奇素数，我们要求对于 $a^{2^{j}m} \equiv 1 (\bmod n)$ ， $j = q, q - 1, \cdots, 0$ 。如果那样，则令 j = 0 得出 $a^{m} \equiv 1 (\bmod n)$ ，这表示算法必定已经由第 8 步返回 prime。假设和算法的结果矛盾，矛盾确定了定理。现在证明我们的断言。由 Fermat 定理，由于 n 是素数，对于 j = q 命题为真，假设命题对于某个 $j, 1 \leqslant j \leqslant q$ 为真，那么对于 j - 1 它也为真，因为

$$
(a ^ {2 ^ {j - 1} m}) ^ {2} = a ^ {2 ^ {j} m} \equiv 1 (\mathrm{mod} n)
$$

蕴含被平方的量是 $\pm 1$ 。事实上，在 $Z_{n}^{*}$ 中，方程 $x^{2} = 1$ 的确只有解 $x = \pm 1$ ，因为在这种情况下 $Z_{n}^{*}$ 是一个域。但是算法的输出中已经排除了-1，因为它必须执行第13步，因此， $a^{2^{j - 1}m} = 1$ 。这样就完成了对断言的证明。得出 $n$ 是合数。

注意上述定理的逆否命题是：如果 $n$ 是素数，那么算法PTEST3返回prime，表示算法在 $n$ 是素数的时候从不出错。

显然，算法 PTEST3 在处理非 Carmichael 数时和算法 PTEST2 同样好。尽管在这里我们不再详述，但可以证明算法 PTEST3 在处理一个 Carmichael 数时出错的概率最大为 1/2，所以，算法对于任意合数出错的可能性最大是 1/2。这样，通过反复测试 k 次，出错的概率最多是 $2^{-k}$ 。如果令 $k = \lceil \log n \rceil$ ，失败的概率变成 $2^{-\lceil \log n \rceil} \leqslant 1/n$ 。换句话说，算法将以至少是 1 - 1/n 的概率给出正确回答，当 n 足够大时可以忽略不计。这产生了我们的最后算法，称为 PRIMALITYTEST。

算法 14.10 PRIMALITYTEST $^{①}$

输入：正奇整数 $n \geqslant 5$ 。

输出：如果 n 是素数，则返回 prime；否则返回 composite。

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
1. $q \leftarrow 0; m \leftarrow n - 1; k \leftarrow \lceil \log n \rceil$
2. repeat {查找 $q$ 和 $m$}
3. $m \leftarrow m / 2$
4. $q \leftarrow q + 1$
5. until $m$ 为奇数
6. for $i \leftarrow 1$ to $k$
7. $a \leftarrow random(2, n - 2)$
8. $x \leftarrow \text{EXPMOD}(a, m, n)$
9. if $x = 0$ then return prime {可能}
</div>

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
10. for $j \leftarrow 0$ to $q - 1$
11. if $x \equiv -1 (\bmod n)$ then return prime {可能}
12. $x \leftarrow x^2 (\bmod n)$
13. end for
14. end for
15. return composite {确定}
</div>

计算算法 PRIMALITYTEST 的运行时间如下。假定在 $O(1)$ 时间内可以生成一个随机整数，repeat 循环耗费 $O(\log n)$ 时间，我们已经在前面证明了第 8 步的耗费是 $\Theta(\log^3 n)$ ，内部 for 循环的平方次数是 $O(\log n)$ ，每次耗费 $O(\log^2 n)$ ，总共是 $O(\log^3 n)$ 时间。因为这重复了 $k = \lceil \log n \rceil$ 次，算法的时间复杂性是 $O(\log^4 n)$ ，下面的定理概括了主要的结果。

定理 14.4 在处理一个奇整数 $n \geqslant 5$ 时，在时间 $O(\log^{4} n)$ 内，算法 PRIMALITYTEST 运作如下。

(1) 如果 $n$ 是素数, 那么它输出prime;

(2) 如果 $n$ 是合数，那么它以至少为 $1 - 1 / n$ 的概率输出 composite。

## 14.9 练习

14.1 设 $p_1, p_2$ 和 $p_3$ 是三个次数分别是 $n, n$ 和 $2n$ 的多项式，写一个随机算法测试是否有 $p_3(x) = p_1(x) \times p_2(x)$ 。

14.2 假定你有一枚硬币，请设计一个有效的随机算法用来生成整数 $1,2,\cdots,n$ 的随机排列，n 为正整数。分析你的算法的时间复杂性。

14.3 仔细定义“随机算法在最坏情况下以期望时间 $T(n)$ 运行”的含义(见1.12节)。

14.4 仔细定义“随机算法在平均期望时间 $T(n)$ 运行”的含义（见 1.12 节）。

14.5 在算法 RANDOMIZEDQUICKSORT 的讨论中曾说过，为算法 QUICKSORT 得到一个 $\Theta(n \log n)$ 期望时间的一种可能性，是通过排列输入元素使它们的次序变成随机的来获得的。描述一个 $O(n)$ 时间的算法，以在用算法 QUICKSORT 处理之前随机地排列输入数组。

14.6 证明当 $k = \lceil n / 2 \rceil$ 时，等式14.1达到最大值。

14.7 考虑对算法 BINARYSEARCH 做如下修改（见 1.3 节），在每次迭代中，随机地选择剩下的位置来代替搜索区间减半。假设在 low 到 high 之间的每一个位置有同样的被算法选择的可能性，比较这种算法和 BINARYSEARCH 的表现。

14.8 设 $A$ 是一个MonteCarlo算法，它的期望运行时间最多是 $T(n)$ ，得出正确解的概率是 $p(n)$ 。假设算法任何解的正确性总是可以在时间 $T'(n)$ 内验证。证明对于同样的问题， $A$ 可以转换成LasVegas算法 $A'$ ，它的期望运行时间最大是 $(T(n) + T'(n)) / p(n)$ 。

14.9 假定一个MonteCarlo算法对于任意的输入，得出正确解的概率都至少是 $1 - \varepsilon_{1}$ 。要把概率增加到至少是 $1 - \varepsilon_{2}$ ，其中 $0 < \varepsilon_{2} < \varepsilon_{1} < 1$ ，需要重复执行算法多少次？

14.10 设 $L = x_{1}, x_{2}, \cdots, x_{n}$ 是一个元素的序列，其中元素 x 恰好出现 k 次 ( $1 \leqslant k \leqslant n$ )

我们要找到一个 $j$ ，使得 $x_{j} = x$ 。考虑重复执行下面的过程直到找到 $x$ 为止。生成一个1到 $n$ 中的随机数 $i$ ，并检查是否 $x_{i} = x$ 。在平均情况下，这种方法和线性查找哪一种方法快？请说明。

14.11 设 $S = \{x_{1}, x_{2}, \cdots, x_{n}\}$ 是一个 $n$ 个正整数的集合，要找出一个元素 $x$ ，如果将 $S$ 排序，元素 $x$ 位于 $S$ 的上半部，或者说 $x$ 大于中值元素。写一个随机算法以 $1 - 1/n$ 的概率来求解这个问题。对于大的 $n$ 值（比如说大于 100 万），将该算法和确定性算法做比较。

14.12 设 $L$ 是一个含有多数元素的 $n$ 个元素的表（见5.7节）。写一个对给出的 $\varepsilon > 0$ ，以 $1 - \varepsilon$ 概率找出这个多数元素的随机算法，考虑到有一个 $O(n)$ 时间的算法来解决这个问题，随机化对这个问题合适吗？

14.13 设 $A, B$ 和 $C$ 是三个 $n \times n$ 矩阵，写一个 $\Theta(n^2)$ 时间的算法测试是否 $AB = C$ ，如果 $AB = C$ ，算法就返回 true。当 $AB \neq C$ 时，算法返回 true 的概率有多大？[提示：令 $x$ 是一个 $n$ 个随机向量，测试 $A(BX) = CX$ 。]

14.14 设 $A$ 和 $B$ 是两个 $n \times n$ 矩阵，写一个 $\Theta(n^2)$ 时间的算法测试是否 $A = B^{-1}$ 。

14.15 考虑 14.7 节中的取样问题，假设在 $n$ 个整数上执行一遍，用概率 $m / n$ 选择其中的每一个，说明结果样本的大小有一个大的变化，因此它的大小可能比 $m$ 小得多或大得多。

14.16 修改 14.7 节中的算法 RANDOMSAMPLING，使得不再需要布尔数组 $S[1 \cdots n]$ ，假定 $n$ 比 $m$ 大得多，比如 $n > m^2$ 。

14.17 练习 14.16 中算法的时间复杂性和空间复杂性是多少?

14.18 证明等式 14.3。

14.19 考虑在算法 14.8 下面定义的 $F_{n}$ , 假设 $n$ 既不是 Carmichael 数也不是素数, 证明 $F_{n}$ 在模 $n$ 乘法运算下构成一个群, 它是 $Z_{n}^{*}$ 的真子群。

14.20 设 $G = (V, E)$ 是有 $n$ 个顶点的连通无向图， $G$ 中的割是一个边的集合，移去这些边使图 $G$ 不连通。 $G$ 中的一个最小割是有最少的边数的割，求解这个问题的直截了当的确定性算法是用最大流最小割定理（见定理16.1）。写一个以合理可能性找出最小割的简单的MonteCarlo算法。

14.21 多重图是允许在顶点对中间有多条边的图。证明在 $n$ 个顶点的多重图中彼此不同的最小割的个数最多是 $n(n - 1) / 2$ （见练习14.20）。

14.22 证明如果有一个 23 人的群体，则他们中两个人有相同生日的可能性至少是 $1/2$ 。

## 14.10 参考注释

随机算法的真正起源是 Rabin 的论文《概率算法》[Rabin(1976)]的发表，论文中给出了两个有效的随机算法：一个是最近点对问题，另一个是素数性测试问题。Solovay 和 Strassen (1977, 1978)的概率算法也是为素数性测试的，是另一个著名的成果。Motwani 和 Raghavan (1995)是有关随机算法的一本综合书籍。这一领域中一些优秀的评述包括 Karp(1991)，Welsh (1983)和 Gupta，Smolka 和 Bhaskar(1994)。随机化快速排序是基于 Hoare(1962)，随机化选择算法来自于 Hoare(1961)，模式匹配算法基于 Karp 和 Rabin(1987)。

对算法 14.10 的修正

在此之前，作者得出结论，存在一个算法 PTEST3，对于任何输入，如果是素数，则输出为 prime；如果是合数，则输出 composite 的概率大于 1/2。

在算法 PRIMALITYTEST 中，作者将重复运行算法 PTEST3 k 次， $k = \left\lceil \log n \right\rceil$ 。

如果 PTEST3 输出为 composite，则输入一定是合数，所以在重复执行的 k 次 PTEST3 中，只要有一次输出为 composite，则停止运行 PRIMALITYTEST，并且输出 composite。而如果 k 次 PTEST3 的输出均为 prime，PRIMALITYTEST 才输出 prime。这样，如果输入是素数，则输出一定为 prime；如果输入是合数，输出 composite 的概率是 $(1/2)^{k}$ ，而作者给出的算法好像在输出上恰恰相反：第 9 步和第 11 步表示只要有一次得到 prime 就输出为 prime，而由第 15 步，只有所有 k 次均为 composite 才输出 composite。所以修改后的算法应该如下（带下划线部分表示修改的部分）。

算法 14.10 PRIMALITYTEST

输入：正奇整数 $n \geqslant 5$ 。

输出：如果 n 是素数，则返回 prime；否则返回 composite。

<div class="mineru-algorithm" style="white-space: pre-wrap; font-family:monospace;">
$q \leftarrow 0; m \leftarrow n - 1; k \leftarrow \lceil \log n \rceil$
repeat {查找 $q$ 和 $m$}
$m \leftarrow m/2$ $q \leftarrow q + 1$
until $m$ 为奇数
for $i \leftarrow 1$ to $k$ $a \leftarrow random(2, n - 2)$ $x \leftarrow \text{EXPMOD}(a, m, n)$
if $x = 1$ then goto 7 for next $i$
for $j \leftarrow 0$ to $q - 1$
if $x \equiv -1 (\bmod n)$ goto 7 for next $i$ $x \leftarrow x^2 (\bmod n)$
end for
return composite {确定}
end for
return prime {可能}
</div>